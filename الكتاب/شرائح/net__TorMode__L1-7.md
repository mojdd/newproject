# شريحة — app/src/main/java/com/bitchat/android/net/TorMode.kt (الأسطر 1–7)

```
1: package com.bitchat.android.net
```
> يُعلن هذا السطر أنّ الملف ينتمي إلى الحزمة (package) باسم `com.bitchat.android.net`. [app/src/main/java/com/bitchat/android/net/TorMode.kt:1]

```
2: 
```
> سطر فارغ. [app/src/main/java/com/bitchat/android/net/TorMode.kt:2]

```
3: enum class TorMode {
```
> يُعرّف هذا السطر صنفاً تعدادياً (enum class) باسم `TorMode` (نمط تور) ويفتح نطاقه بقوس مفتوح. [app/src/main/java/com/bitchat/android/net/TorMode.kt:3]

```
4:     OFF,
```
> يُعرّف هذا السطر قيمة تعدادية باسم `OFF` (مُطفأ) متبوعة بفاصلة. [app/src/main/java/com/bitchat/android/net/TorMode.kt:4]

```
5:     ON
```
> يُعرّف هذا السطر قيمة تعدادية باسم `ON` (مُشغَّل). [app/src/main/java/com/bitchat/android/net/TorMode.kt:5]

```
6: }
```
> إغلاق نطاق. [app/src/main/java/com/bitchat/android/net/TorMode.kt:6]

```
7: 
```
> سطر فارغ. [app/src/main/java/com/bitchat/android/net/TorMode.kt:7]
