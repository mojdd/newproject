# الخطة — التصميم التقني والبحثي لشبكة الأربعينية 🛠️📚

> هذا ملف **الخطة**: بحث تقني موسّع + تصميم قابل للتنفيذ. الفكرة والقصة بملف
> **[القصة](القصة.md)**.
>
> **الحالة:** توثيق تصميم وبحث — **ماكو كود أندرويد بعد** (البيئة الحالية ما تبني
> أندرويد؛ التفاصيل بالقسم ١٥). كل رقم تقني وكل اسم API هنا **مبني على مصادر
> حقيقية** مذكورة بقسم المراجع (القسم ١٧).

---

## ملخّص تنفيذي (الصورة بنصف صفحة)

- **المشكلة:** الشبكة الخلوية **تنهار تحت الكثافة** (تكسر ٤ أسقف معاً: PRBs، اتصالات
  RRC ~٩٠٠/خلية، قناة التحكّم PDCCH، وعاصفة إشارات RACH)، والأربعينية أكثف تجمّع
  سنوي (~٢٢ مليون على ممر ٨٠ كم، ٣ أيام، حتى ٤٨°م).
- **الفكرة:** تلفونات الناس نفسها = الشبكة. الرسالة تنطّ تلفون-تلفون عبر BLE. **كل
  ما زاد الزحام، اشتغلت أحسن** (عكس البرج).
- **البنية:** سبعة أنظمة صعبة («وحوش»): الخلفية، النقل، التوجيه، التسليم، التشفير،
  البوابة، التخزين. لو فشل أول وحشين، التطبيق ميت.
- **القرارات المعمارية المبنية على البحث:** فيضان متحكّم به (Ni et al. + Meshtastic
  SNR-suppression) · TTL=7 · seen-set **دقيق** (مو Bloom وحده) · store-carry-forward
  لـ DTN (Spray-and-Wait) للمسافات الطويلة · sealed boxes (libsodium) للتشفير
  اللاتزامني · Foreground Service `connectedDevice` (معفى من سقف ٦ ساعات) للبقاء حي ·
  تغليف مفاتيح بالـ Keystore (لأنه ما يدعم 25519).
- **الدروس من ٩ تطبيقات:** Bitchat مرجع معماري لكنه انكسر (MITM)؛ Bridgefy انكسر
  مرتين (لا تبتكر تشفير، لا تضغط ثم تشفّر)؛ FireChat أثبت الكثافة لكنه مات بلا تشفير
  ولا استدامة؛ Briar نموذج الأمان الصح (Cure53)؛ Meshtastic أفضل انضباط فيضان.
- **الصدق:** الفيضان الحيّ ينفع **محلياً (~٢٠٠م، ٧ قفزات)**؛ الـ٨٠كم تحتاج
  store-carry-forward + مراسي ثابتة. القيد الحقيقي **التشبّع والبطارية وصمود الخلفية
  ضد OEM** — يحتاج معايرة ميدانية، لا ندّعي «شغّال» قبل ما يُبنى ويُختبَر.
- **الحالة:** **منطق نقي (`:core`) قابل للاختبار الآن على JVM** (توجيه، تشفير، تنسيق،
  تسليم)؛ قشرة أندرويد (BLE/خلفية/Room/UI) تحتاج بيئة فيها SDK وأجهزة.

---

## الفهرس

0. كيف تقرأ هذا الملف
1. المشكلة — لماذا تنهار الشبكة الخلوية تحت الزحام (بالأرقام)
2. الفكرة والقرار المعماري والستاك
3. المعمارية الطبقية والمصطلحات
4. 🪨 وحش ١ — البقاء حي بالخلفية (عميق)
5. 🪨 وحش ٦ — نقل BLE و Wi-Fi (عميق)
6. 🪨 وحش ٢ — التوجيه (Controlled Flooding + DTN)
7. 🪨 وحش ٣ — التخزين والتسليم
8. 🪨 وحش ٤ — التشفير والهوية والأمان
9. 🪨 وحش ٥ — البوابة الانتهازية
10. 🪨 وحش ٧ — التخزين المحلي
11. نموذج البيانات وتنسيق الرزمة (Wire Format)
12. دروس من التطبيقات المشابهة (Bitchat، Briar، Bridgefy، FireChat…)
13. نموذج التهديد (Threat Model)
14. الاختبار ومحاكاة الكثافة
15. البيئة والحالة وخطة البناء
16. الافتراضات وسجل المخاطر
17. المصطلحات والمراجع
- **الملاحق:** أ (أرقام BLE) · ب (قاتلو OEM) · ج (التوجيه + Bloom) · د (خوارزميات
  DTN) · هـ (Noise/libsodium) · و (سرد التطبيقات المشابهة) · ز (انهيار الخلوي) ·
  ح (قائمة فحص التنفيذ) · ط (طبقة الميزات) · ي (قرارات مفتوحة) · خاتمة

---

## ٠. كيف تقرأ هذا الملف

التطبيق قلبه **سبعة أنظمة صعبة** نسمّيها "وحوش". كل وحش نظام قائم بذاته. لكل وحش:

- **المشكلة:** شنو الصعب بالضبط.
- **الحل:** القرار المعماري والآليات.
- **تفاصيل تقنية:** أسماء APIs، أرقام، قيود إصدارات أندرويد، أمثلة.
- **اختبار النجاح:** متى نقول "اشتغل".
- **قابل لاختبار JVM؟** — جزء كبير من المنطق نقدر نختبره بدون أجهزة أندرويد.
- **الصدق:** القيود اللي ما تنحل، نقولها صراحة.

**المبدأ الحاكم لكل شي: أوفلاين أولاً.** ولا طبقة تتوقف بسبب غياب النت.

---

## ١. المشكلة — لماذا تنهار الشبكة الخلوية تحت الزحام (بالأرقام)

### ١.١ حجم الأربعينية

زيارة الأربعينية (النجف → كربلاء) توصف بأنها **أكبر تجمّع بشري سنوي بالعالم**.
الأرقام الرسمية (هيئة العتبات):

| السنة | عدد الزوار (تقريبي) |
|------|---------------------|
| 2016 | 11.2 مليون |
| 2019 | 15.2 مليون |
| 2022 | 21.6 مليون |
| 2023 | ~22.0 مليون |
| 2024 | 21.5 مليون |
| 2025 | أكثر من 22 مليون |

- المسير الرئيسي: **~80 كم سيراً على الأقدام، ~3 أيام**، معلّم بـ **1,452 عمود
  مرقّم تفصل بينها 50 متر**.
- كربلاء مدينة عادتها ~2 مليون، تنتفخ لـ **~21.5 مليون** خلال الزيارة.

> الخلاصة: كثافة بشرية هائلة على ممر خطّي ضيّق لأيام. هذا **بالضبط** الظرف اللي
> تنهار فيه الشبكة الخلوية، وبالضبط الظرف اللي تشتغل فيه شبكة الـ mesh **أحسن**.

### ١.٢ ليش تنهار الشبكة الخلوية تحت الكثافة؟ (٤ أسقف تنكسر بنفس اللحظة)

البرج الخلوي مو سعته لا نهائية. تحت الزحام الشديد تنكسر **أربعة حدود معاً**:

1. **موارد الراديو محدودة (PRBs):** خلية LTE عندها عدد ثابت من "بلوكات الموارد"؛
   كل ما زاد المستخدمون، انهار نصيب كل واحد من السرعة.
2. **سقف عدد المتصلين فعلياً (RRC):** اختبار هندسي واقعي (WirelessMoves) بيّن إن
   قاعة فيها **~4,000 جهاز نشط على خليتين** حافظت على **~1,800 اتصال RRC متزامن ≈
   900 لكل خلية** فقط — البرج ما يقدر يمسك أكثر من عدد محدود من "السياقات".
3. **تشبّع قناة التحكّم (PDCCH):** القناة اللي تجدول المستخدمين كل 1ms تتشبّع؛ بخلية
   10MHz واقعياً **~16 مستخدم/TTI** لمن الإشارة ضعيفة (والزحام يضعّف الإشارة).
4. **عاصفة الإشارات (RACH):** لمن آلاف الأجهزة تحاول الدخول/التسليم بنفس اللحظة
   (موجة زحام، أو الكل يمسك تلفونه بلحظة وحدة)، قناة الدخول العشوائي **تتصادم
   وتفشل الاتصالات**.

نتيجة موثّقة: بالفعاليات المزدحمة، **نسبة فشل الاتصال توصل ~400 ضعف** المعدّل
العادي، والذروة تصير بـ **اللحظات المتزامنة** (هدف بمباراة، أو منتصف الليل برأس
السنة) — لأن الكل يتصرّف بنفس اللحظة فيضرب الأسقف الأربعة سوا.

### ١.٣ شنو يسوون المشغّلون؟ ولماذا ما يكفي؟

بالحج 2024 (هيئة الاتصالات السعودية): **44.8 مليون مكالمة**، **5.79 بيتابايت
بيانات**، **أكثر من 6,200 برج (منها 4,000+ برج 5G)**. وبكومب ميلا 2025 بالهند:
**328 برج جديد + 78 برج متنقّل (COWs) + 150 خلية صغيرة**.

يعني الحل التقليدي = **ضخّ بنية تحتية ضخمة ومؤقتة**. غالي، صعب، وما يغطّي كل ممر
الـ80 كم. وحتى مع كل هذا، الذروات المتزامنة تكسر الشبكة.

> **البديل الذي نقترحه:** ما نضيف أبراج — نخلّي **تلفونات الناس نفسها** هي الشبكة.
> كل ما زاد الزحام، زادت العقد، فصارت الشبكة أقوى. عكس البرج تماماً.

---

## ٢. الفكرة والقرار المعماري والستاك

### ٢.١ الفكرة بجملة

> تلفونات الناس تحكي مع بعض مباشرة عبر BLE/Wi-Fi، والرسالة **تنطّ من تلفون لتلفون**
> (multi-hop) حتى توصل بعيد، بدون برج ولا إنترنت. ولمن أي تلفون يلمس نت، يصير
> **بوابة** يرفع المسموح لسيرفر.

### ٢.٢ ليش mesh لا-مركزي (وليش مو خادم/خلوي)؟

- **الخلوي ينهار تحت الكثافة** (القسم ١) — والأربعينية ذروة كثافة.
- **mesh يستفيد من الكثافة:** كل عقدة قريبة = قافز محتمل. الكثافة وقود، مو عبء.
- **لا-مركزي = ما فيه نقطة فشل وحدة**؛ ما ينتظر بنية تحتية حكومية على ممر 80 كم.

### ٢.٣ الستاك (التقنيات) ومبرّراته

| الطبقة | الاختيار | السبب |
|--------|----------|-------|
| المنصّة | **Android native** | الجمهور ~كله أندرويد؛ BLE + الخلفية يحتاجون تحكّم native كامل |
| اللغة | **Kotlin** | لغة أندرويد الرسمية |
| الواجهة | **Jetpack Compose** | واجهات حديثة، بناء سريع |
| التزامن | **Coroutines + Flow** | إدارة شبكة/DB/حالات غير متزامنة |
| المدى القصير | **BLE (GATT)** | استهلاك بطارية منخفض، أساس الـ mesh المستمر |
| الباندويث الأعلى | **Wi-Fi Aware (NAN) / Wi-Fi Direct** | نقل بيانات أكبر؛ الدعم يختلف بين الأجهزة |
| التشفير | **libsodium** (`lazysodium`) | E2E مُختبَر؛ sealed boxes تناسب mesh لا-تزامني |
| التخزين | **Room + SQLCipher** | قاعدة محلية مشفّرة |
| الخلفية | **Foreground Service** (`connectedDevice`) | يبقي التلفون عقدة relay وهو بالجيب |

**قاعدة ملزمة:** ماكو backend-as-a-service ولا اعتماد على سيرفر للتشغيل. السيرفر
**وجهة مزامنة انتهازية فقط**.

---

## ٣. المعمارية الطبقية والمصطلحات

```
        تلفون زائر (عقدة mesh)
   ┌──────────────────────────────┐
   │  UI (Compose)                │
   ├──────────────────────────────┤
   │  Features (موقع، رسائل، مبيت…)│
   ├──────────────────────────────┤
   │  Crypto / Identity           │  ← وحش ٤
   ├──────────────────────────────┤
   │  Mesh: routing + forward     │  ← وحش ٢،٣
   ├──────────────────────────────┤
   │  Transport: BLE + Wi-Fi Aware│  ← وحش ٦
   ├──────────────────────────────┤
   │  Background survival         │  ← وحش ١
   ├──────────────────────────────┤
   │  Local storage               │  ← وحش ٧
   └──────────────────────────────┘
            ▲ لمن تتوفّر نت → بوابة (وحش ٥)
            ▼
     ☁️ سيرفر حكومي (تجميع + تمرير blobs مشفّرة)
```

**المصطلحات:**

- **عقدة (Node):** تلفون زائر مشارك، له `NodeId` مشتق من مفتاحه العام.
- **وسيط (Relay):** عقدة تمرّر رزمة مو موجّهة لها (تشوف blob مشفّر فقط).
- **بوابة (Gateway):** عقدة لمست نت مؤقتاً فترفع المسموح للسيرفر.
- **رزمة (Packet):** وحدة البيانات اللي تنتقل بالـ mesh.
- **موثوق (Trusted):** جهة بادلتها مفتاحك وجهاً لوجه (QR) — يشوف اسمك وموقعك.
- **غريب (Stranger):** عقدة relay مجهولة — تمرّر بلا ما تقرأ.
- **قفزة (Hop):** انتقال الرزمة من تلفون للي بعده.
- **Controlled flooding:** إعادة بث مُتحكَّم بها بدل routing منظّم.

---

## ٤. 🪨 وحش ١ — البقاء حي بالخلفية (Background Survival)

### ٤.١ المشكلة

التلفون لازم يبقى عقدة relay وهو **بالجيب والشاشة مطفية**. لكن أندرويد مصمَّم ليقتل
الخلفية ويوفّر بطارية، **وكل مصنّع (OEM) يقتل بطريقة مختلفة وغير موثّقة**. أنت
تحارب النظام.

**اختبار النجاح:** التلفون يبقى يمرّر رسائل بعد ٣٠ دقيقة من إطفاء الشاشة، وبعد
إعادة تشغيل الجهاز.

### ٤.٢ Foreground Service — العمود الفقري

نشغّل **Foreground Service** دائمة بإشعار ثابت. النوع الصحيح لتطبيقنا:

- **`android:foregroundServiceType="connectedDevice"`** بالـ manifest.
- صلاحية `FOREGROUND_SERVICE_CONNECTED_DEVICE` (تُمنح وقت التثبيت، بلا سؤال).
- ثابت `startForeground`: `ServiceInfo.FOREGROUND_SERVICE_TYPE_CONNECTED_DEVICE`.
- الوصف الرسمي للنوع: "التفاعل مع أجهزة خارجية عبر Bluetooth/NFC/USB/شبكة" — وهذا
  بالضبط relay الـ BLE.

> **أهم حقيقة معمارية بكل الوحش:** نوع `connectedDevice` **ما عليه مؤقّت توقّف**.
> أندرويد 15 (API 35) فرض سقف **٦ ساعات/٢٤ ساعة** على نوعَي `dataSync` و
> `mediaProcessing`، لكن `connectedDevice` و`location` و`mediaPlayback` **معفيّة**.
> لذلك relay الـ mesh **لازم** يستعمل `connectedDevice`، **لا** `dataSync`. (ونوع
> `shortService` مسقوف ~٣ دقايق وما يصير sticky — ما ينفعنا.)

**متطلبات أندرويد 14 (API 34):**
- كل Foreground Service **لازم** يعلن `foregroundServiceType` بالـ manifest +
  الصلاحية المطابقة، وإلا `MissingForegroundServiceTypeException`.
- لازم **تعلن نوع الخدمة بـ Play Console** وتبرّره، وإلا يُرفض التطبيق.
- لتشغيل خدمة `connectedDevice` لازم تتحقق واحدة على الأقل من: صلاحيات BLE الممنوحة
  وقت التشغيل (`BLUETOOTH_CONNECT/ADVERTISE/SCAN`) أو `CHANGE_NETWORK_STATE` إلخ.

**إعدادات أساسية:**
- `onStartCommand` يرجّع **`START_STICKY`** (يطلب إعادة إنشاء الخدمة لو قُتلت).
- إشعار فوري عبر `setForegroundServiceBehavior(FOREGROUND_SERVICE_IMMEDIATE)`
  (وإلا يتأخّر ~١٠ ثوانٍ على API 30+).
- على أندرويد 13+ (API 33) يحتاج صلاحية `POST_NOTIFICATIONS` عشان الإشعار يبيّن.

### ٤.٣ منع البدء من الخلفية (API 31+)

من API 31، **ما تقدر تشغّل** `startForegroundService()` وأنت بالخلفية — يرمي
`ForegroundServiceStartNotAllowedException`. الاستثناءات اللي نعتمد عليها:

- المستخدم فتح التطبيق مؤخراً، أو ضغط إشعار.
- **منبّه (`AlarmManager`) دقيق انطلق** — مفيد لإعادة التشغيل.
- **استقبال بثّ الإقلاع** (`BOOT_COMPLETED`، `LOCKED_BOOT_COMPLETED`).
- **المستخدم عطّل تحسين البطارية** للتطبيق → **إعفاء شامل** (لذلك مهم نقنع المستخدم
  يضيفنا للقائمة البيضاء).
- Companion Device Manager.

### ٤.٤ الصمود عبر إعادة التشغيل (BootReceiver)

- صلاحية `RECEIVE_BOOT_COMPLETED`، ومستقبِل يسمع:
  - `BOOT_COMPLETED` — بعد الإقلاع الكامل وأول فتح قفل (تخزين كامل متاح).
  - `LOCKED_BOOT_COMPLETED` — أبكر، أثناء Direct Boot قبل فتح القفل (يحتاج
    `directBootAware="true"`، وبس تخزين الجهاز المشفّر متاح).
  - `QUICKBOOT_POWERON` — إجراء OEM غير قياسي للإقلاع السريع (نضيفه للتغطية لكنه غير
    موثوق).
- **قيد مهم:** التطبيق لازم **يكون فُتح مرة وحدة على الأقل** من المستخدم؛ لو
  بحالة "stopped" (ما انفتح أبداً، أو force-stop) **ما يستقبل** `BOOT_COMPLETED`
  إطلاقاً. وقتل OEM = force-stop يكتم الإقلاع لين الفتح اليدوي.
- على أندرويد 15: بثّ الإقلاع ممنوع يشغّل أنواع `dataSync/camera/...` لكن
  **`connectedDevice` مسموح** — وهذا اختيارنا.

### ٤.٥ Doze و App Standby — شنو يتقيّد بالضبط

الجهاز يدخل **Doze** لمن: مفصول عن الشاحن + ثابت + الشاشة مطفية + غير مستخدم. وقتها
النظام:

1. **يوقف الشبكة** كلياً.
2. **يتجاهل wake locks** (حتى PARTIAL_WAKE_LOCK).
3. **يؤجّل المنبّهات العادية** — يفير فقط `setAndAllowWhileIdle()` /
   `setExactAndAllowWhileIdle()` بمعدّل **~مرة كل ٩ دقايق**.
4. **ما يشغّل مسح Wi-Fi**.
5. **ما يشغّل JobScheduler/WorkManager**.

**أثر BLE:** Doze يوقف الراديو فعلياً بين "نوافذ الصيانة". لكن **خدمة `connectedDevice`
الفعّالة تبقي عملية التطبيق خارج قيود Doze على مستوى التطبيق** أثناء تشغيلها — وهذا
سبب اعتمادنا على خدمة دائمة بدل مهام دورية. **نوافذ الصيانة تصير أندر كل ما طال
الخمول** (أول نافذة ~ساعة، ثم تتباعد). الحركة أو تشغيل الشاشة يخرج من Doze فوراً.

**إعفاء البطارية:**
- `PowerManager.isIgnoringBatteryOptimizations(pkg)` يفحص.
- `ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` يطلب إعفاء بنقرة وحدة.
- **سياسة Play تقيّد هذا** — مسموح بس لو Doze يكسر وظيفة جوهرية أو ما تقدر تستعمل
  FCM. **تطبيقنا حالة مؤهَّلة صراحةً** (تطبيق سلامة/مراسلة لا يقدر يستعمل FCM لأن
  ماكو نت أصلاً) — نوثّق هذا التبرير بـ Play Console.

### ٤.٦ قاتلو الخلفية من المصنّعين (OEM Killers) — أصعب جزء

موقع [dontkillmyapp.com](https://dontkillmyapp.com) يرتّب المصنّعين؛ الصينيون
وسامسونغ تاريخياً الأسوأ. **ما ينحل بالكود لحاله — يحتاج إرشاد المستخدم.** خطوات
لكل مصنّع:

- **شاومي (MIUI/HyperOS):** فعّل **Autostart** (التشغيل التلقائي) من تطبيق Security
  → اضبط موفّر البطارية للتطبيق على "بلا قيود" → بالـ Recents اسحب بطاقة التطبيق
  لين يطلع قفل.
- **هواوي (EMUI):** Battery → **Launch manager** → أطفئ "الإدارة التلقائية" →
  فعّل الثلاثة: Auto-launch، Secondary launch، Run in background.
- **أوبو (ColorOS):** فعّل التطبيق بـ **Startup Manager** + أطفئ تحسين البطارية +
  ثبّته بالـ Recents.
- **فيفو:** Battery → **Background power management** → أطفئ الإدارة التلقائية،
  وفعّل التشغيل بالخلفية يدوياً.
- **ون بلس (OxygenOS):** Battery optimization → **Don't optimize** (ينتبه: ون بلس
  **يرجّعها بصمت** أحياناً، فثبّت التطبيق بالـ Recents بعد) + أطفئ Deep optimization.
- **سامسونغ (One UI):** Battery → **Background usage limits** → أضف التطبيق لـ
  **Never sleeping apps** + أطفئ "Put unused apps to sleep".

**حقيقة عبر كل المصنّعين:** ماكو شي من هذا يُضبَط برمجياً. التطبيق لازم **يكشف
المصنّع** (`Build.MANUFACTURER`) و**يرشد المستخدم** بخطوات مخصّصة، ويعيد فحص
`isIgnoringBatteryOptimizations()` دورياً (بعضهم يرجّع القيود بصمت).

### ٤.٧ WakeLock والمنبّهات والمراقبة الذاتية (Heartbeat)

- **WakeLock:** `PARTIAL_WAKE_LOCK` **فقط أثناء معالجة رزمة فعلية** (استلام/تمرير)،
  مع `acquire(timeout)`/`release()` سريع. لا تمسكه دائماً (يحرق البطارية). يحتاج
  صلاحية `WAKE_LOCK`. تذكّر: Doze يتجاهله أصلاً؛ الخدمة الأمامية هي اللي تبقيك حي.
- **AlarmManager للنبض/إعادة التشغيل:** استعمل `setAndAllowWhileIdle()` (غير دقيق،
  آمن سياسياً) لاختراق Doze. المنبّهات الدقيقة (`SCHEDULE_EXACT_ALARM`) **مرفوضة
  افتراضياً** على أندرويد 13+ وحسّاسة سياسياً — نتجنّبها.
- **WorkManager:** أدنى دورة **١٥ دقيقة**، ويتأجّل بـ Doze. **ما ينفع للمسح
  المستمر** — بس كـ "حارس" دوري كل ١٥ دقيقة يفحص: الخدمة حيّة؟ لو لا، يعيد تشغيلها.

**معمارية النبض (حزام + حمّالات):** خدمة `connectedDevice` دائمة (الأساس) + حارس
WorkManager كل ١٥ دقيقة + إعادة تشغيل بـ AlarmManager عند `onTaskRemoved` (لمن
يمسح المستخدم التطبيق من Recents) + BootReceiver. **كل طبقة قابلة للفشل، فنكدّسها.**

### ٤.٨ موازنة الطاقة (Power Profiles)

| الوضع | متى | السلوك |
|------|-----|--------|
| `AGGRESSIVE` | شحن/بطارية عالية | دورات مسح أنشط، استعمال Wi-Fi Aware عند توفّره |
| `BALANCED` | افتراضي | مسح متوازن |
| `LOW_POWER` | بطارية منخفضة | خفّض تردد المسح/الإعلان، BLE فقط، أطفئ Wi-Fi إلا للضرورة، أبقِ استقبال الطوارئ |

### ٤.٩ واجهة مقترحة (Kotlin)

```kotlin
interface MeshLifecycleController {
    fun start()                            // يبدأ Foreground Service
    fun stop()
    val state: StateFlow<ServiceState>
    fun onBatteryLevelChanged(pct: Int)    // يضبط power profile
}
enum class PowerProfile { AGGRESSIVE, BALANCED, LOW_POWER }
```

### ٤.١٠ حالات اختبار

- [ ] الخدمة تبقى حية بعد ٣٠ دقيقة شاشة مطفية (شاشة على/مطفية/Doze).
- [ ] الخدمة ترجع بعد reboot.
- [ ] الخدمة ترجع بعد مسحها من Recents.
- [ ] WakeLock ما يُمسَك إلا أثناء معالجة فعلية (قياس بالـ profiler).
- [ ] شاشة الإرشاد تظهر الخطوات الصح حسب `Build.MANUFACTURER`.

### ٤.١١ الصدق — شنو ما نقدر نضمنه

1. **ماكو صمود مضمون 100%.** START_STICKY وFGS وwake locks — ولا واحد يصمد ضد
   منظّف OEM عدواني أو "Clear all" بتأكيد.
2. **إعدادات Autostart للمصنّعين يدوية فقط، وترجع بصمت** أحياناً.
3. **Doze يؤجّل حتى مهام التطبيق المُعفى** (يحرّر الشبكة والـ wakelock بس).
4. **قيود بدء FGS من الخلفية (API 31+)** تضيق باستمرار.
5. **خطر سياسة Play** على صلاحيات الإعفاء والمنبّهات الدقيقة.

> **التأطير الأمين:** الهدف "**نرفع احتمال البقاء حي ونتعافى بسرعة لو قُتلنا**"، مو
> "نضمن تشغيل ٢٤/٧". نكدّس FGS + حارس + boot + قائمة بيضاء للمستخدم، ونعامل كل
> طبقة كأنها قابلة للفشل.

**يحتاج أجهزة حقيقية للاختبار.** milestone: **M1**.

---

## ٥. 🪨 وحش ٦ — نقل BLE و Wi-Fi (BLE / Wi-Fi Transport)

### ٥.١ المشكلة

BLE مصمَّم لأجهزة صغيرة وحمولات ضئيلة، **مو لنقل بيانات كثيفة بين مئات الأجهزة**.
لازم نطوّعه ليصير ناقل mesh.

**اختبار النجاح:** جهازان يكتشفون بعض ويتبادلون رزمة كاملة (أكبر من MTU، مجزّأة
ومعاد تجميعها) بنجاح، بالخلفية.

### ٥.٢ الإعلان عن الحضور (Advertising)

نستعمل الإعلان لـ **beacon حضور فقط** (NodeId + flags)، **مو لنقل الرزم**.

**الإعلان القديم (Legacy):**
- الحمولة القصوى **٣١ بايت**. كل حقل (AD structure) يكلّف **٢ بايت overhead** (طول
  + نوع).
- حقل Flags يكلّف ٣ بايت (إلزامي للإعلان القابل للاتصال). يبقى **~٢٨ بايت**.
- Manufacturer Data (نوع `0xFF`): ٢ overhead + ٢ معرّف شركة = **٤ بايت ثابتة**
  قبل بياناتك. UUID 128-bit يأكل ١٨ بايت.
- **Scan response** = ٣١ بايت إضافية تُرجَع عند `SCAN_REQ` → يقرّب يضاعف السعة.
- **ميزانية واقعية لـ beacon:** مع Flags + حقل Manufacturer واحد يبقى **~٢٤ بايت**
  لمحتواك (NodeId + عدد قفزات + flags قدرة بوابة).
- لو زادت البيانات: `onStartFailure(ADVERTISE_FAILED_DATA_TOO_LARGE = 1)`.

**الإعلان الممتد (Extended، BLE 5، API 26):**
- حتى **٢٥٥ بايت** بـ PDU واحد، و**~١٦٥٠ بايت** بالتسلسل (chained).
- PHYs: `PHY_LE_1M=1` (متوافق)، `PHY_LE_2M=2` (أسرع، قناة ثانوية فقط)،
  `PHY_LE_CODED=4` (مدى أطول، ~125–500 kbps).
- نفحص الدعم: `isLeExtendedAdvertisingSupported()`، `isLe2MPhySupported()`،
  `getLeMaximumAdvertisingDataLength()` (يرجّع ٣١ لو ماكو دعم ممتد، وإلا حتى ١٦٥٠).

**APIs:** `BluetoothLeAdvertiser` (من `BluetoothAdapter.getBluetoothLeAdvertiser()`):
`startAdvertising(settings, data, scanResponse, callback)` (API 21) أو
`startAdvertisingSet(...)` (API 26 للممتد). أوضاع الإعلان:
`ADVERTISE_MODE_LOW_POWER` (~1000ms)، `BALANCED` (~250ms)، `LOW_LATENCY` (~100ms).

### ٥.٣ المسح (Scanning)

**`BluetoothLeScanner`** (API 21): `startScan(filters, settings, callback)`.

**أوضاع المسح (`ScanSettings`) ودورة العمل التقريبية (من AOSP):**

| الوضع | القيمة | نافذة/فاصل | الدورة |
|------|-------|-----------|--------|
| `SCAN_MODE_OPPORTUNISTIC` | -1 | يركب على مسح غيره | سلبي |
| `SCAN_MODE_LOW_POWER` | 0 (افتراضي) | ~500ms / 5000ms | ~10% |
| `SCAN_MODE_BALANCED` | 1 | ~2000ms / 5000ms | ~40% |
| `SCAN_MODE_LOW_LATENCY` | 2 | ~5000ms / 5000ms | ~100% |

**قيود حرجة على المسح بالخلفية:**
- **قاعدة "٥ مرات بـ ٣٠ ثانية":** من أندرويد 7 (API 24)، ما تقدر تستدعي
  `startScan()` أكثر من **٥ مرات بنافذة ٣٠ ثانية**. السادسة تُرفَض (على API 33+
  بالخطأ `SCAN_FAILED_SCANNING_TOO_FREQUENTLY = 6`). **العلاج:** اعتبر المسح جلسة
  وحدة طويلة، لا تبدأ/توقف بكثرة.
- **مسح بلا فلتر + شاشة مطفية = ممنوع:** لو مسحت بلا `ScanFilter` وانطفت الشاشة،
  أندرويد **يوقف تسليم النتائج**. **العلاج:** لازم فلتر (Service UUID مالنا) +
  خدمة أمامية.
- **قاعدة ٣٠ دقيقة:** المسح المستمر **يُخفَّض تلقائياً لـ OPPORTUNISTIC بعد ~٣٠
  دقيقة** → لازم ندوّر المسح (نوقف/نبدأ) دورياً قبل الـ٣٠ دقيقة.
- **مسح بـ PendingIntent** (`startScan(filters, settings, pendingIntent)`) يصمد عبر
  موت العملية، لكن يحتاج فلتر غير فارغ.

### ٥.٤ اتصالات GATT (النقل الفعلي)

النقل الحقيقي عبر **GATT connection** بين عقدتين. أندرويد يصير **سيرفر وعميل بنفس
الوقت**.

**تصميم خدمة GATT للـ mesh** (خدمة واحدة بـ UUID 128-bit + خاصيّتين):
- **خاصية الاستقبال (RX):** `PROPERTY_WRITE` / `PROPERTY_WRITE_NO_RESPONSE` —
  الجار يكتب رزم فيها. استعمل `WRITE_TYPE_NO_RESPONSE` للسرعة (عدة رزم بنفس حدث
  الاتصال).
- **خاصية الإرسال (TX):** `PROPERTY_NOTIFY` — الجار يشترك، ونحن ندفع الرزم له.

> **خطأ شائع:** تفعيل الإشعارات يحتاج **خطوتين** (لو سويت وحدة بس، يفشل بصمت):
> ١) `setCharacteristicNotification(ch, true)` محلياً.
> ٢) كتابة الـ **CCCD (UUID `0x2902`)** على السيرفر بقيمة `ENABLE_NOTIFICATION_VALUE`.

**Notify مقابل Indicate:** Notify غير مؤكَّد، عدة إشعارات بالطابور بنفس حدث الاتصال
→ throughput أعلى بكثير → **الأنسب للـ mesh**. Indicate مؤكَّد لكن **واحد فقط
معلّق بنفس الوقت** → بطيء.

**سقف الاتصالات المتزامنة:** واقعياً **~٤–٨ اتصالات، يعتمد على الشريحة** (~٧ شائع).
**قيد منفصل خطير:** الـ stack يسمح بـ **٣٢ عميل GATT مسجّل** كحد أقصى؛ كل
`connectGatt()` يسجّل واحد، ولو ما استدعيت `close()` **تتسرّب التسجيلات** وبعد ٣٢
ما تقدر تتصل أبداً. **`close()` إلزامي** بعد كل قطع/فشل.

**الخطأ ١٣٣ (`GATT_ERROR = 0x85`):** خطأ عام، غالباً timeout.
- `autoConnect=false`: "اتصل الحين"، يحاول ~٣٠ ثانية ثم يفشل بـ ١٣٣، وواحد معلّق
  بس بنفس الوقت.
- `autoConnect=true`: "اتصل لمن يظهر"، أبطأ لكن **أوثق بكثير** — الحل الموصى به ضد
  ١٣٣ المزمن.
- **سَلسِل عمليات GATT (وحدة وحدة):** الـ stack أحادي الخيط فعلياً — اتصال، اكتشاف
  خدمات، MTU، قراءة، كتابة... كل وحدة تنتظر callback قبل التالية. **طابور أوامر
  FIFO** هو الحل القياسي.

### ٥.٥ MTU والحمولة المفيدة

- **MTU الافتراضي = ٢٣ بايت**، والـ ATT header ٣ بايت → **حمولة مفيدة = ٢٠ بايت**.
- `requestMtu(int)` حتى **٥١٧** (`GATT_MAX_MTU_SIZE`)، النتيجة بـ `onMtuChanged`.
- حمولة الإشعار/الكتابة = **MTU − ٣** → عند ٥١٧ ≈ **٥١٤ بايت**.
- **خصوصية أندرويد 14+:** لمن العميل يطلب MTU، أندرويد **يفاوض على ٥١٧ تلقائياً
  بغضّ النظر عن القيمة، ويتجاهل طلبات MTU اللاحقة** (issuetracker #288211591).
  خطّط لـ ٥١٧.
- **Data Length Extension (DLE، BLE 4.2):** يرفع حمولة طبقة الربط من ٢٧ لـ ٢٥١
  بايت → حتى **~٣.٥× throughput**. لازم نرفع MTU و DLE **معاً**.

**throughput واقعي:**
- LE 1M PHY: مع MTU افتراضي ~**٨٥ kbps**؛ مع DLE + MTU كبير ~**٤٠٠ kbps**؛ تلفون
  بأحسن حالاته ~**٧٢٠–٨٠٠ kbps**.
- LE 2M PHY (BLE 5): ذروة واقعية **~١.٠–١.٤ Mbps** (مع DLE + MTU 247).
- **المهم بالترتيب:** MTU+DLE > 2M PHY > فاصل الاتصال (~١٥ms جيد) >
  WRITE_NO_RESPONSE/notify فوق المؤكَّد.

### ٥.٦ التجزئة وإعادة التجميع (Fragmentation)

الرزم > (MTU − overhead) تُقسَّم fragments. كل fragment:
`[ packetId(16B) | fragIndex(2B) | fragTotal(2B) | chunk... ]`. المستقبل يجمّع حسب
`packetId` لمن تكتمل `fragTotal`؛ timeout يُسقِط الناقص.

> **منطق التجزئة/التجميع نقي قابل لاختبار JVM** (بايتات داخلة/طالعة)؛ فقط الراديو
> الفعلي أندرويد.

**بديل أعلى throughput:** **L2CAP CoC** (channels موجّهة للاتصال، أندرويد API 29+
عبر `BluetoothSocket`/`createL2capChannel`) تعطي تدفّق بايتات مباشر بدون overhead
الـ GATT — مفيد للنقل الكبير.

### ٥.٧ مجدول الاتصالات (Connection Scheduler)

- التلفون يمسك ~٤–٨ اتصالات؛ **scheduler يدوّر بينها**: يتصل بجار → ينقل ما عنده →
  يقطع (`close()`!) → ينتقل لجار ثاني.
- يفضّل الجيران بـ RSSI أقوى وآخر ظهور أحدث.
- يوازن: تغطية أكبر عدد جيران ↔ عدم إرهاق الراديو/البطارية.

### ٥.٨ Wi-Fi Aware (NAN) للباندويث الأعلى

**المبدأ:** BLE للتحكّم والإشارة (دائم، رخيص)، **Wi-Fi للبيانات الكبيرة** (صور
خرائط، دفعات بوابة). نفس نمط Briar و Bridgefy.

**الواقع (القيد الكبير):** رغم إن Wi-Fi Aware من أندرويد 8 (API 26)، **الدعم
العتادي متقطّع وحسب المصنّع**. تاريخياً الأجهزة المعتمدة الوحيدة كانت **Pixel 3/3XL
و Galaxy Note 10**؛ كثير أجهزة ترجّع `false` من `FEATURE_WIFI_AWARE`. **ما نفترض
وجوده** — نكشفه ونرجع لـ BLE.

**APIs:** `WifiAwareManager` (`isAvailable()`, `attach()`)، نشر/اشتراك
(`publish`/`subscribe`)، ثم مسار بيانات **IPv6 link-local socket** عبر
`WifiAwareNetworkSpecifier` + `ConnectivityManager.requestNetwork` →
`getPeerIpv6Addr()` → socket TCP عادي. PSK يعطي تشفير طبقة الربط.

**الأرقام:** throughput **عشرات لمئات Mbps (~١٠٠–٦٠٠× BLE)**، لكن **طاقة أعلى
بكثير** (~٠.٥–٠.٧ واط إرسال مقابل ملّي-واطات لـ BLE). لذلك **نشغّله بس وقت النقل
الكبير ونطفّيه بعد**.

**Wi-Fi Direct (P2P):** بديل احتياطي، دعمه أوسع (API 14) لكنه **أخرق للـ mesh**:
مجموعة وحدة بمالك واحد (نجمة مو mesh)، تكوين بطيء (ثوانٍ)، وأحياناً نافذة تأكيد
نظام — قاتلة للتكوين التلقائي.

**استراتيجية التصعيد:**
1. الاكتشاف والإشارة = BLE دائماً.
2. صعّد لـ Wi-Fi فقط عند: **حمولة كبيرة** / **نقل مستمر** / **جار مستقر** فترة كافية.
3. فضّل Wi-Fi Aware لو الطرفين يدعمونه، وإلا Wi-Fi Direct، وإلا ابقَ على BLE.
4. **اقطع Wi-Fi بعد الدفعة** ورجّع لـ BLE لتوفير البطارية.

### ٥.٩ جدول الجيران (Neighbor Table)

| الحقل | الاستعمال |
|------|-----------|
| `NodeId` | الهوية |
| `rssi` | قوة الإشارة → ترتيب الجدولة |
| `lastSeen` | آخر ظهور → تقادم/شطب |
| `batteryHint` | إن شورِكَت → انتخاب البوابة |
| `gatewayCapable` | flag من الإعلان |

### ٥.١٠ الصلاحيات

- **API ≤ 30:** `BLUETOOTH`, `BLUETOOTH_ADMIN`, و`ACCESS_FINE_LOCATION` (المسح
  يحتاج الموقع من API 23!)، و`ACCESS_BACKGROUND_LOCATION` للمسح بالخلفية (API 29).
- **API 31+:** `BLUETOOTH_SCAN` (مع `neverForLocation` لتجنّب طلب الموقع)،
  `BLUETOOTH_ADVERTISE`, `BLUETOOTH_CONNECT`.

### ٥.١١ واجهة مقترحة

```kotlin
interface MeshTransport {                     // أندرويد (BLE/Wi-Fi Aware)
    fun startAdvertising(beacon: PresenceBeacon)
    fun startScanning()
    val neighbors: StateFlow<List<Neighbor>>
    suspend fun send(packet: ByteArray, to: PeerId)   // يتولّى MTU + fragmentation
    val incoming: Flow<ReceivedPacket>
}
object Fragmenter {                            // pure Kotlin — JVM-testable
    fun split(packet: ByteArray, maxChunk: Int): List<Fragment>
    fun reassemble(fragments: List<Fragment>): ByteArray?   // null لو ناقص
}
```

### ٥.١٢ حالات اختبار

- وحدات (JVM): `reassemble(split(p)) == p` لأحجام مختلفة؛ fragment ناقص → `null`؛
  ترتيب وصول مبعثر يُجمَّع صح.
- أجهزة: اكتشاف جارين؛ نقل رزمة > MTU؛ نقل بالخلفية؛ scheduler يدوّر بين عدة جيران؛
  التحقق من `close()` (ما نوصل سقف ٣٢).

### ٥.١٣ الصدق — قيود BLE بمقياس الأربعينية

- سقف الاتصالات (~٧) يعني كل عقدة تخدم جيران محدودين بنفس اللحظة.
- آلاف المُعلِنين بالمدى → فيضان مسح، ضغط على الـ callbacks، وفشل اتصالات.
- فشل القفزات بالزحام الكثيف **موثّق فعلياً** بـ Bridgefy (القسم ١٢).

**النقل الفعلي يحتاج أجهزة؛ منطق Fragmenter قابل لاختبار JVM.** milestone: **M1**.

---
## ٦. 🪨 وحش ٢ — التوجيه (Controlled Flooding + DTN)

### ٦.١ المشكلة

عندك عشرات/مئات الجيران لكل عقدة، وطوبولوجيا تتغير كل ثانية (الناس تمشي). routing
بجداول مسارات **مستحيل** بهالفوضى. ولو كل واحد يعيد بث كل رزمة → **عاصفة بث
(broadcast storm)** تسدّ الشبكة وتحرق البطاريات.

**اختبار النجاح:** رسالة تقطع ≥٥ قفزات بلا تكرار لا نهائي وبلا انفجار trafik.

### ٦.٢ مشكلة عاصفة البث — الأساس النظري (Ni et al., MobiCom 1999)

الورقة المرجعية «The Broadcast Storm Problem in a Mobile Ad Hoc Network» (Ni،
Tseng، Chen، Sheu) تشرح ليش الفيضان الأعمى (كل عقدة تعيد البث مرة) يخرب عبر **ثلاث
آفات**:

1. **التكرار (Redundancy):** العقدة تستلم نفس الرزمة من جيران كثير، وأغلب الإعادات
   تغطّي منطقة مغطّاة أصلاً.
2. **التنازع (Contention):** الجيران اللي استلموا يعيدون البث **بنفس اللحظة تقريباً**
   فيتنازعون على نفس الوسط.
3. **التصادم (Collision):** التنازع الكثيف + غياب backoff للبث + مشكلة الطرف المخفي
   → تصادمات، والبث غير مؤكَّد فالخسائر ما تُستدرَك.

**التحليل الكمّي (قلب الورقة):** لمن عقدة تعيد البث، المساحة **الجديدة** اللي
تغطّيها (خارج مساحة المُرسِل):
- **أقصى تغطية إضافية ~٦١٪** من مساحة العقدة (عند الحافة، d→r).
- **التغطية الإضافية المتوقّعة ~٤١٪ فقط** (توزيع منتظم).
- **EAC(k)** = التغطية الإضافية المتوقّعة بعد ما سمعت العقدة الرزمة **k مرات**.
  تنهار بسرعة: بعد سماعها **٣–٤ مرات**، التغطية الإضافية المتوقّعة **تحت ~٥٪** →
  الإعادة بعدها هدر شبه خالص. **هذا تبرير عتبة العدّاد `C = 3 أو 4`.**

### ٦.٣ الأربع آليات اللي نتبنّاها

**آلية ١ — Message ID (منع التكرار):** كل رزمة لها معرّف فريد = هاش على (المحتوى +
المصدر + nonce/timestamp). للـ dedup عبر الشبكة كلها.

**آلية ٢ — TTL (حدّ القفزات):**
- **القاعدة الأساسية:** TTL لازم **≥ قطر الشبكة** (أطول مسار) عشان الرسالة توصل الكل.
  أقل = توصل مبتورة؛ أعلى = هدر باندويث وتكرار.
- **القيم بالأنظمة الحقيقية (للمقارنة):** AODV `NET_DIAMETER=35` مع expanding ring
  search (`TTL_START=1`, `TTL_INCREMENT=2`, `TTL_THRESHOLD=7`)؛ Meshtastic
  hop_limit **٣ افتراضي / ٧ أقصى** (حقل ٣-بت)؛ Bitchat **TTL=7** (وبالكثافة يحدّه
  لـ ٥ لاحتواء الفيضان)؛ Bluetooth SIG Mesh TTL حقل ٧-بت (٠–١٢٧، نموذجي ٥–٧)؛ DTN
  BPv7 Hop Count Block (نوع ١٠، حدّ ١–٢٥٥).
- **الدرس:** كل الأنظمة الواقعية تستقر على **٣–٧ قفزات** لأن تكلفة الفيضان تكبر
  بسرعة. **نبدأ بـ `DEFAULT_TTL=7`، `EMERGENCY_TTL` أعلى، ونعايره بالمحاكاة.**

**آلية ٣ — Seen-set (ذاكرة الرزم المرئية) + درس Bloom المهم:**

السؤال: «شفت هالرزمة قبل؟» — لو «نعم» نكتمها، لو «لا» نعيد بثها. الآن لاحظ خطأين:
- **إيجابي كاذب (False Positive):** البنية تقول «شفتها» وهي **جديدة** → **نكتم رسالة
  جديدة فعلاً = خسارة تسليم دائمة**. ⚠️ **الخطأ الخطير.**
- **سلبي كاذب (False Negative):** البنية تقول «جديدة» وهي **مكرّرة** → إعادة بث
  زائدة وحدة = هدر بسيط. **غير مؤذٍ** (الفيضان مكرّر أصلاً).

> **النتيجة الحاسمة:** **Bloom filter العادي عنده إيجابيات كاذبة وما عنده سلبيات
> كاذبة أبداً** — أي يطلّع **بالضبط الخطأ الخطير** أبداً الخطأ الحميد! فلو استعملناه
> كسلطة وحيدة لقرار «شفتها»، راح **يُسقِط رسائل جديدة بصمت**.
>
> **قرارنا:** نستعمل **مجموعة ID دقيقة (LRU محدودة بالزمن)** كسلطة (صفر إيجابي
> كاذب؛ خطؤها الوحيد = نسيان قديم → إعادة بث حميدة). نقدر نستعمل Bloom **بس
> كمُسرِّع غير حاسم** أمام الفحص الدقيق. هذا اللي تسويه أنظمة gossip الإنتاجية مثل
> libp2p gossipsub (LRU time cache، `duplicate_cache_time` افتراضي دقيقة وحدة).
>
> **رياضيات Bloom (لو استعملناه كمسرّع):** نسبة الإيجابي الكاذب `(1−e^(−kn/m))^k`،
> العدد الأمثل للهاشات `k=(m/n)·ln2`، والبتات لكل عنصر `≈1.44·log2(1/p)` — يعني
> **~٩.٦ بت/عنصر لـ ١٪، ~١٤.٤ بت/عنصر لـ ٠.١٪**.

نخزّن الـ seen-set بجدول DB (`SeenMessageCache`) للصمود عبر إعادة التشغيل، ونقلّمه
حسب `SEEN_SET_TTL` (٢٤–٤٨ ساعة).

**آلية ٤ — Suppression (تثبيط البث) — قلب منع العاصفة:**

قبل الإعادة، **انتظر تأخير عشوائي قصير (RAD) + أنصت**. لو سمعت جار بثّ نفس الرزمة
خلال الانتظار، **اسكت** (counter-based). الـ RAD العشوائي `[0, T_max]` يسوي شغلين:
(١) **يبعثر** الإعادات فيكسر التصادم المتزامن، (٢) يفتح **نافذة إنصات** نعدّ فيها
النسخ.

**درس Meshtastic (نتبنّاه):** نافذة التنافس (CW) تعتمد على **قوة الإشارة (SNR/RSSI)**:
- إشارة ضعيفة (جار **بعيد**) → CW صغيرة → **يعيد البث أول** (يدفع الفيضان للخارج).
- إشارة قوية (جار **قريب**) → CW كبيرة → ينتظر، ولمن يسمع البعيد بثّها **يسكت**.
- هذا يعظّم التغطية لكل قفزة ويقلّل التصادم. `CWmin=2, CWmax=7`، والتأخير
  `delay = (2·CWmax) + random(0, 2^CWsize)` بوحدات slot-time.

### ٦.٤ منطق المعالجة (pseudocode)

```text
on receive(packet, fromPeer):
    if packet.id in seenSet:                  # آلية ٣ (LRU دقيقة)
        recordDuplicateHeard(packet.id)       # يغذّي عدّاد suppression
        cancelMyPendingRebroadcast(packet.id) # لو كنت ناوي أعيدها → اسكت
        return
    seenSet.add(packet.id)
    if isForMe(packet): deliverUp(packet)     # وحش ٣

    packet.ttl -= 1                           # آلية ٢
    if packet.ttl <= 0: return

    if packet.priority == EMERGENCY:
        rebroadcast(packet)                   # طوارئ: طابور منفصل، تأخير أدنى
        return

    cw = cwFromSignal(rssi(fromPeer))         # Meshtastic-style
    delay = (2*CW_MAX) + random(0, 2^cw)
    heardCount = 0
    wait(delay) while counting duplicates → heardCount
    if heardCount >= SUPPRESS_THRESHOLD:      # جيران كفوا التغطية
        return
    rebroadcast(packet)
```

### ٦.٥ نظرية Gossip (Haas, Halpern, Li 2002) — ليش الفيضان الاحتمالي يشتغل

بديل/مكمّل: كل عقدة تعيد البث **باحتمال p**. النتيجة المدهشة (نظرية النفاذية
percolation):
- فيه **عتبة `p₀`**: فوقها الرسالة **تنتشر لكل الشبكة**، وتحتها **تموت بسرعة**.
- السلوك **ثنائي (bimodal)**: إما يوصل القليل جداً أو الأغلب — ماكو وسط.
- **`p ≈ 0.65–0.75` يكفي** لوصول شبه كامل، و**يوفّر ~٣٥٪** رسائل مقابل الفيضان.
- **GOSSIP1(p,k):** بثّ باحتمال ١ لأول `k` قفزات (k≈4) ثم p — يمنع الموت قرب المصدر.
- **`p₀` يعتمد على الكثافة:** شبكة أكثف (درجة أعلى) = `p₀` أقل = نقدر نستعمل p أصغر.

> **لتطبيقنا:** الكثافة هائلة بالأربعينية (٤–٥ شخص/م²)، فنقدر نستعمل **suppression
> عدوانية + p منخفض** بأمان — المشكلة عندنا **كثرة الوسطاء مو قلّتهم**.

### ٦.٦ معاملات قابلة للضبط (نُعاير بالمحاكاة)

| المعامل | قيمة ابتدائية | ملاحظة |
|--------|---------------|--------|
| `DEFAULT_TTL` | 7 | مثل Bitchat؛ يُضبط بالكثافة |
| `EMERGENCY_TTL` | 10–12 | أعلى للطوارئ |
| `DENSE_TTL_CAP` | 5 | حدّ بالكثافة العالية (درس Bitchat) |
| `CW_MIN / CW_MAX` | 2 / 7 | نافذة التنافس (Meshtastic) |
| `SUPPRESS_THRESHOLD` | 3 | C=3 (تحليل EAC(k)) |
| `GOSSIP_P` (اختياري) | 0.65 | لو فعّلنا الفيضان الاحتمالي |
| `SEEN_SET_TTL` | 24–48h | عمر إدخال الـ seen-set |

### ٦.٧ أنواع الرزم والأولوية

| النوع | الانتشار |
|------|----------|
| **Broadcast** | فيضان عادي (بيانات تجميعية للكل) |
| **Unicast** | يُفاض، لكن **يفكّ تشفيرها الهدف فقط** (وحش ٤) |
| **Receipt** | فيضان موجّه نحو `originNodeId` (وحش ٣) |

**طابور طوارئ منفصل** لرزم SOS: TTL أعلى، بلا تأخير suppression، أولوية إرسال دائمة.

### ٦.٨ واجهة مقترحة وحالات اختبار

```kotlin
interface Router {
    suspend fun onPacketReceived(packet: Packet, fromPeer: PeerId): RouteDecision
    fun enqueueOriginated(packet: Packet)
}
sealed interface RouteDecision {
    object Drop : RouteDecision
    data class DeliverLocal(val packet: Packet) : RouteDecision
    data class Rebroadcast(val packet: Packet, val afterDelayMs: Long) : RouteDecision
}
```

- [ ] رزمة معروفة → `Drop`. — [ ] `ttl==1` تُسلَّم لكن ما تُعاد. — [ ] سماع نسختين
  ضمن النافذة → suppression. — [ ] EMERGENCY → بلا تأخير، طابور منفصل. — [ ]
  seen-set دقيق → **ماكو إسقاط رسالة جديدة**. — [ ] سلسلة ≥٥ عقد توصل بلا حلقات.

**منطق نقي قابل لاختبار JVM كامل.** milestone: **M2**.

---

## ٧. 🪨 وحش ٣ — التخزين والتسليم (Store-and-Forward + DTN)

### ٧.١ المشكلة

ماكو سيرفر يگلك «وصلت». فكيف تعرف، وأنت بلا نت، إن رسالتك وصلت؟ وكيف توصل رسالة
لشخص على بُعد ٨٠ كم لمن ماكو سلسلة قفزات حيّة متصلة؟

**اختبار النجاح:** دورة ⏳→✓ تشتغل كاملة أوفلاين (المصدر يستلم خبر الوصول عبر الـ
mesh بلا أي نت).

### ٧.٢ المبدأ: DTN (شبكات تتحمّل التأخير) — Store-Carry-Forward

المسافات الطويلة **ما تُحلّ بقفزات حيّة** (٨٠ كم = آلاف القفزات، مستحيل بالفيضان).
الحل: **خزّن–احمل–مرّر**: العقدة تخزّن الرزمة، **تحملها وهي تمشي**، وتمرّرها لمن
تلاقي جيران جدد. الحركة نفسها تنقل الرسالة. هذا أساس DTN (RFC 4838/5050/9171).

**نماذج التوجيه DTN (من الأبحاث) ومتى نستعملها:**

| النموذج | الفكرة | الكلفة | مناسب لنا؟ |
|---------|--------|--------|------------|
| **Epidemic** (Vahdat-Becker) | لمن عقدتان تلتقيان تتبادلان «summary vectors» وتنقلان الناقص | تسليم ممتاز، **هدر هائل** | للطوارئ فقط |
| **Spray-and-Wait** | المصدر يوزّع **L نسخة** بس؛ binary: كل عقدة تعطي نصف نسخها | محدود (≤ L−1 إرسال) | ✅ الأساس |
| **PRoPHET** | احتمال تسليم لكل وجهة، يمرّر للأرجح | متوسط | لو فيه بنية اجتماعية |
| **MaxProp** | أولوية إرسال/إسقاط + ACK فيضان | متقدّم | مرجع للترتيب |

> **قرارنا:** **Spray-and-Wait الثنائي** كأساس (يحدّ النسخ فلا ينفجر الهدر)،
> `L ≈ 20–25٪` من تقدير عدد العقد، مع **ترقية انتهازية** (مثل Spray-and-Focus:
> العقدة الأخيرة تمرّر نسختها لعقدة أقرب للهدف). الطوارئ تستعمل epidemic (تسليم أهم
> من الكفاءة).

**ليش هذا يشتغل بالأربعينية:** الكثافة عالية = **التقاءات كثيرة جداً** = نموذج
النسخ يزدهر. **القيد الحقيقي = الذاكرة (buffer) مو الاتصال.** (محاكاة الملعب:
epidemic تسليم ~٠.١٨–٠.٣٨ والباقي يضيع تحت ضغط الذاكرة؛ زمن ~٥٧٤–١٠٣٣ ثانية).
لذلك **حذف الرزم المؤكَّدة إلزامي** (تحت).

### ٧.٣ آلة الحالة (State Machine)

```
QUEUED_LOCAL ──> IN_MESH ──┬──> DELIVERED_PEER   (وصلت العقدة الموثوقة الهدف)
                            └──> UPLOADED_SERVER  (ارتفعت لبوابة → سيرفر)
                                       │
                                       ▼
                                     ACKED  ──> EXPIRED  (انتهت أو وصل ACK)
```
| الحالة | المعنى | مؤشر |
|--------|--------|------|
| `QUEUED_LOCAL` | مخزونة، ما دخلت الشبكة | ⏳ |
| `IN_MESH` | تُفاض الآن | ⏳ |
| `DELIVERED_PEER` | استلمها الهدف | ✓ |
| `UPLOADED_SERVER` | رفعتها بوابة | ✓ |
| `ACKED` | رجع إيصال للمصدر | ✓✓ |
| `EXPIRED` | انتهت — تتوقف وتُشطَّب | — |

### ٧.٤ التخزين والتمرير الدوري والانتهاء

- كل رزمة تُخزَّن محلياً وتُعاد بثّها **دورياً** (مو مرة) — الجيران يتغيرون، جار جديد
  يحملها أبعد.
- **انتهاء زمني مزدوج (من DTN):** **حدّ قفزات (TTL)** يحدّ المدى + **عمر زمني
  (lifetime)** يحدّ التخزين. بعد **٢٤–٤٨ ساعة** أو وصول **ACK** → الرزمة تتوقف
  وتُشطَّب. BPv7 يستعمل «Bundle Age Block» (عمر بالملّي ثانية) للعقد بلا ساعة دقيقة.

### ٧.٥ الإيصالات (ACK Flooding / الـ "Vaccine")

- لمن يستلم **الهدف** (أو **السيرفر** عبر بوابة)، يُولَّد **ACK موقَّع** ويُفاض
  **رجوعاً نحو `originNodeId`** — فحتى لو المصدر بلا نت، يوصله «وصلت».
- **مهم (درس MaxProp/epidemic):** الـ ACK يلعب دورين — يطمّن المصدر **و**يخلّي كل
  عقدة تشوفه **تحذف نسختها** من الرزمة المؤكَّدة (تنظيف الذاكرة استباقي = "vaccine"/
  "anti-packet"). بدونه الذاكرة تمتلئ والتسليم ينهار.
- الإيصال رزمة `Receipt` تخضع لنفس TTL/dedup/suppression، **موقّعة** عشان المصدر
  يتأكد إنها أصلية مو مزوّرة.

### ٧.٦ مكافحة الإعادة (Anti-Replay)

من تصميم IPsec (RFC 6479): **رقم تسلسل تصاعدي لكل مصدر + نافذة منزلقة** (افتراضي
٦٤). نقبل المعرّفات داخل النافذة وغير المرئية؛ نرفض الأقدم من النافذة والمكرّر.
**نفس فحص «شفتها؟» يخدم الهدفين: منع الإعادة + كتم الفيضان.** الـ expiry يحدّ نافذة
الإعادة وحجم الـ seen-set.

### ٧.٧ واجهة وحالات اختبار

```kotlin
interface ForwardingStore {
    suspend fun enqueue(packet: Packet)                 // QUEUED_LOCAL
    suspend fun markState(id: MessageId, s: MsgState)
    suspend fun dueForRebroadcast(now: Long): List<Packet>
    suspend fun onReceiptReceived(receipt: Receipt)     // يطابق ويصعّد + يحذف
    suspend fun reapExpired(now: Long): Int             // EXPIRED + شطب
    fun observe(id: MessageId): Flow<MsgState>          // للـ UI
}
```
- [ ] رزمة جديدة → `QUEUED_LOCAL`. — [ ] receipt مطابق → `ACKED` ويتوقف الانتشار
  **وتُحذف النسخ**. — [ ] تجاوز 48h → `EXPIRED`. — [ ] receipt بتوقيع غير صالح →
  يُهمَل. — [ ] `dueForRebroadcast` يرجّع غير المنتهية وغير المؤكَّدة فقط.

**آلة الحالة والمطابقة والـ expiry منطق نقي قابل لاختبار JVM؛ الـ persistence
أندرويد.** milestone: **M3**.

---

## ٨. 🪨 وحش ٤ — التشفير والهوية والأمان (Crypto / Identity / Security)

### ٨.١ المشكلة

رسالتك تمرّ بتلفونات **غرباء**. كيف تمنعهم يقرونها؟ وكيف يتأكد المستلم إن الرزمة
منك مو من منتحل؟

**اختبار النجاح:** relay ما يقدر يقرأ محتوى unicast؛ والتوقيع يُتحقَّق صح (وتزوير
التوقيع يُرفَض).

### ٨.٢ الهوية (libsodium)

كل عقدة تولّد محلياً (أول تشغيل) زوجين مفاتيح:

| الزوج | الخوارزمية | الحجم | الغرض |
|-------|------------|------|-------|
| توقيع | **Ed25519** | مفتاح عام ٣٢ بايت، توقيع ٦٤ بايت | يثبت أصل الرزمة |
| تشفير | **X25519** | ٣٢ بايت | sealed boxes |

`NodeId` = اشتقاق من المفتاح العام للتوقيع. المفتاح الخاص **لا يطلع أبداً**.

### ٨.٣ التشفير E2E عبر Sealed Boxes — وليش هو الأنسب

نستعمل **`crypto_box_seal`** (libsodium): يشفّر لمفتاح عام **بلا handshake تفاعلي**.
- **overhead ٤٨ بايت** (مفتاح عابر ٣٢ + MAC ١٦).
- يخفي **هوية المرسِل** عن غير المستلم.

> **ليش sealed box مثالي للـ mesh (مبني على بحث Noise):** أنماط Noise التفاعلية
> (مثل **XX** المتبادل، أو **IK**) تحتاج **الطرفين متّصلين بنفس اللحظة** لإكمال ٣
> رسائل handshake. بشبكة عقد متحركة تختفي بأي لحظة ورسائل تُخزَّن-وتُمرَّر عبر غرباء،
> **هذا مستحيل**. النمط أحادي الاتجاه **`N`** بـ Noise = بالضبط sealed box: تشفّر
> للمستلم بمجرد ما تعرف مفتاحه العام، رسالة وحدة مكتفية بذاتها، بلا رد ولا تزامن.
> الثمن: ماكو مصادقة مرسِل افتراضية (لذلك **نوقّع** بـ Ed25519) وماكو طزاجة تفاعلية
> (لذلك **anti-replay** منفصل، القسم ٧.٦).

آلية كل رزمة unicast: (١) **شفّر** الحمولة لمفتاح **المستلم** العام. (٢) **وقّع**
(الـ header + الحمولة) بمفتاح **المرسِل** الخاص. (٣) الوسطاء يمرّرون **blob + توقيع**
بلا ما يفكّونه.

### ٨.٤ ماذا يُوقَّع (درس من Bitchat)

التوقيع يغطّي (الـ header ما عدا `ttlHops` + الـ body). **`ttlHops` خارج التوقيع**
لأن كل relay يُنقِصه — لو كان داخله لانكسر التوقيع. (Bitchat يسوي نفس الشي: يستثني
TTL من البايتات الموقّعة.) الباقي محمي ضد العبث.

### ٨.٥ تخزين المفاتيح — قيد Android Keystore (مهم)

> **Android Keystore ما يدعم Ed25519/X25519** — يدعم بس RSA و **EC بمنحنيات NIST
> (P-256...)** و AES و HMAC. ماكو ثابت `KEY_ALGORITHM` لـ 25519.

**النمط المعتمد (تغليف المفتاح / Envelope Encryption):**
1. ولّد مفاتيح libsodium بالذاكرة.
2. بالـ Keystore ولّد **مفتاح AES-256-GCM رئيسي غير قابل للتصدير** مدعوم بالعتاد
   (StrongBox إن وُجد، وإلا TEE — مع التقاط `StrongBoxUnavailableException` والرجوع
   لـ TEE).
3. شفّر (لُفّ) مفاتيح 25519 الخاصة بمفتاح AES، واخزن الـ ciphertext (+ IV ١٢ بايت +
   tag ١٦ بايت).
4. عند الحاجة، فُكّ بالذاكرة فقط وامسحها بعد الاستعمال.

> ملاحظة: مكتبة Jetpack `androidx.security.crypto` (`EncryptedSharedPreferences`،
> `MasterKey`) **أُهملت أبريل ٢٠٢٥** — نستعمل Keystore مباشرة.

### ٨.٦ التخزين المشفّر (SQLCipher) — ربطه بالمفاتيح

- **SQLCipher 4:** تشفير صفحة-بصفحة بـ **AES-256-CBC** + **HMAC-SHA512** لكل صفحة،
  اشتقاق المفتاح **PBKDF2-HMAC-SHA512 بـ ٢٥٦,٠٠٠ تكرار**، salt ١٦ بايت بأول الملف.
- **الربط الأمثل:** ولّد **مفتاح DB عشوائي ٣٢ بايت**، لُفّه بمفتاح Keystore الرئيسي،
  واعطِه SQLCipher عبر **raw-key PRAGMA** (`x'<64 hex>'`) لتخطّي PBKDF2 (المفتاح
  عالي الإنتروبيا أصلاً). فتح DB يربط فكّ التشفير **بعتاد الجهاز**.
- المكتبة الحالية: `net.zetetic:sqlcipher-android` (الحزمة `net.zetetic.database`،
  factory `SupportOpenHelperFactory`). الأداء: ~٥–١٥٪ overhead بعد الفتح.

### ٨.٧ إنشاء الثقة (منع MITM)

تبادل DH خام **عرضة لـ MITM** (مهاجم بالوسط). الحل: تأكيد عبر **قناة خارجية
(out-of-band)** المهاجم ما يتحكّم بها.

| المستوى | الوصف | الأمان |
|---------|-------|--------|
| **TOFU** (ثقة أول استعمال) | اقبل المفتاح أول مرة، ثبّته | ضعيف بأول لقاء، آمن بعده |
| **QR وجهاً لوجه** | امسح مفتاح الطرف الكامل بصرياً | **يمنع MITM** (المهاجم مو بالقناة البصرية) |

QR يحمل المفتاح العام كامل (٣٢ بايت) فما فيه خطأ نسخ — أقوى من بصمة مقصوصة. (نموذج
Signal: «أرقام الأمان» = ٦٠ رقم = بصمتا الطرفين مدموجتين.) **للموثوقين: QR؛
للغرباء: TOFU + يمرّرون blob فقط.**

### ٨.٨ أمان السيرفر (البوابة)

- **مفتاح السيرفر العام مثبّت (pinned)** ومدمج بالتطبيق + **TLS cert pinning** →
  ضد سيرفر منتحل و MITM.
- الرفع **موقّع** بمفتاح العقدة.

### ٨.٩ دروس من الكوارث الأمنية (ماذا **لا** نسوي)

تطبيقات سابقة انكسرت — نتعلّم منها صراحةً:

- **Bridgefy (انكسر مرتين، Albrecht et al. 2020/2022):** بلا مصادقة → انتحال أي
  مستخدم؛ معرّفات بالنص الصريح → رسم الشبكة الاجتماعية؛ **ضغط ثم تشفير** → تسريب
  بقناة جانبية (CRIME)؛ **قنبلة فك ضغط (zip bomb)** → تعطيل الشبكة كلها برسالة
  وحدة؛ وحتى بعد تبنّي libsignal، خلل **TOCTOU** كسر السرّية بنسبة ~٥٠٪.
- **Bitchat (2025):** أُطلق بلا مراجعة أمنية → باحث (Radocea) أثبت **MITM/انتحال**
  بسبب مصادقة هوية مكسورة؛ ومفاتيح عامة ثابتة → **تتبّع BLE**.
- **FireChat:** **بلا تشفير إطلاقاً** بأوج استعماله بالاحتجاجات → كارثة خصوصية.

> **قواعدنا الملزمة:** (١) **لا تبتكر تشفيرك** — استعمل libsodium المُختبَر. (٢)
> **مصادقة حقيقية** (توقيع Ed25519) + تأكيد QR. (٣) **لا تضغط ثم تشفّر** بيانات
> يأثّر فيها المهاجم. (٤) **حُدّ فكّ الضغط** (ضد zip bomb). (٥) **بطّن الرسائل**
> (أحجام ثابتة ٢٥٦/٥١٢/١٠٢٤...) ضد تحليل الحجم. (٦) **دوّر المعرّفات** ضد التتبّع.
> (٧) **مراجعة أمنية مستقلة قبل الإطلاق** (مثل Briar مع Cure53) — مو بعده.

### ٨.١٠ واجهة وحالات اختبار

```kotlin
interface CryptoEngine {                       // pure JVM عبر lazysodium-java
    fun generateIdentity(): Identity
    fun seal(plaintext: ByteArray, recipientPub: PublicKey): ByteArray
    fun unseal(ciphertext: ByteArray, me: Identity): ByteArray?
    fun sign(message: ByteArray, me: Identity): Signature
    fun verify(message: ByteArray, sig: Signature, senderPub: PublicKey): Boolean
}
interface KeyVault {                            // Android (Keystore + SQLCipher)
    fun storeEncrypted(id: Identity); fun load(): Identity?
}
```
- [ ] `unseal(seal(m, pub), me) == m`. — [ ] عقدة ثالثة → `null`. — [ ]
  `verify(m, sign(m,A), A.pub)==true`. — [ ] تعديل بايت → `verify==false`. — [ ]
  توقيع B ضد A.pub → `false`.

**منطق التشفير قابل لاختبار JVM عبر `lazysodium-java`؛ تخزين المفاتيح أندرويد.**
milestone: **M4**.

---
## ٩. 🪨 وحش ٥ — البوابة الانتهازية (Opportunistic Gateway)

### ٩.١ المشكلة

لمن عدة عقد تلمس نت بنفس الوقت، لو كلها ترفع نفس الداتة → **تكرار وحرق بطاريات**.

**اختبار النجاح:** عقدة واحدة (الأقوى) ترفع، الباقي يسكت، والسيرفر يستبعد أي تكرار
متبقّي عبر Message IDs.

### ٩.٢ انتخاب البوابة (Gossip Election)

- العقدة اللي تلمس نت **تعلن** قدرتها بالـ mesh (gossip خفيف).
- تُختار **الأقوى** بين القريبات اللي عندها نت:
  - **مستوى البطارية** (الأعلى يفضّل).
  - **جودة النت/قوة الإشارة**.
  - كاسر تعادل: أعلى `NodeId` (deterministic).
- الباقي **يسكت**؛ لو البوابة المنتخبة اختفت، يُعاد الانتخاب لثاني أقوى.
- **لا-مركزي بالكامل** — ماكو منسّق مركزي.

### ٩.٣ رفع Idempotent (طبقة حماية ثانية)

حتى لو فشل الانتخاب وارتفعت داتة مكررة، السيرفر **يستبعد التكرار** عبر `MessageId`
الفريد. فالانتخاب **تحسين** (يقلّل الهدر) **مو شرط صحة** — الصحة مضمونة بالـ
idempotency. هذا نفس مبدأ MaxProp: الـ ACKs والـ dedup يمنعون التكرار حتى لو
الفيضان أرسل نسخ.

### ٩.٤ ماذا يُرفع — جدول ملزم ⚠️

| النوع | يُرفع؟ | كيف |
|------|--------|-----|
| تجميعي (عدد، كثافة، وجبات، ماء، تواليتات، إسعاف، صحة عامة، حركة مواكب) | ✅ | **مكشوف** للسيرفر |
| شفافية المواكب (دعم، صرف، موردين، طاقة) | ✅ | مكشوف — شفافية كيان خدمي |
| رسائل/مواقع الأفراد | 🔒 | **blobs مشفّرة E2E تمرّ عبوراً فقط** — السيرفر ساعي بريد، ما يقرأ |
| أي بيانات فردية بالاسم | ⛔ | **لا تُجمع أصلاً** (الخطوط الحمراء) |

> السيرفر **وجهة تجميعية + ساعي بريد للـ blobs المشفّرة** — لا أكثر.

### ٩.٥ البروتوكول والأمان

- **رفع دفعي (batch):** يجمع المخزون التجميعي + blobs الشخصية ويرفعهم دفعة.
- **موقّع** بمفتاح العقدة (Ed25519) + **server pinning + TLS cert pinning** (وحش ٤).
- السيرفر يردّ **ACKs** بالـ MessageIds → رزم وحش ٣ تصير `UPLOADED_SERVER` ثم
  `ACKED`، والإيصالات **تفيض رجوعاً** بالـ mesh.

### ٩.٦ واجهة وحالات اختبار

```kotlin
interface GatewayManager {
    fun onInternetAvailable(); fun onInternetLost()
    val isElectedGateway: StateFlow<Boolean>
}
interface ServerUploader { suspend fun uploadBatch(b: UploadBatch): UploadResult }
data class UploadBatch(val aggregates: List<AggregateRecord>,
                       val opaqueBlobs: List<EncryptedPacket>)
```
- [ ] بين ٣ عقد عدها نت، الأعلى score → `isElectedGateway=true` والباقي `false`.
- [ ] البوابة اختفت → يُعاد الانتخاب. — [ ] رفع نفس الـ batch مرتين → السيرفر
  يستبعد التكرار. — [ ] **ماكو بيانات فردية بالاسم** بالـ `UploadBatch`.

**منطق الانتخاب قابل لاختبار JVM؛ الرفع الفعلي (HTTPS+pinning) يحتاج شبكة.**
milestone: **M5**.

---

## ١٠. 🪨 وحش ٧ — التخزين المحلي (Local-First Storage)

### ١٠.١ المشكلة والمبدأ

كل شي يشتغل ويُحفظ **بلا نت**. القاعدة المحلية **مصدر الحقيقة الوحيد**؛ النت ما
"يجيب" بيانات — بس **يصرّف المخزون** لمن يتوفّر.

**اختبار النجاح:** كل البيانات تُكتب/تُقرأ مشفّرة محلياً؛ تنظيف دوري يشطّب المنتهي
بلا نزف ذاكرة.

### ١٠.٢ التقنية

- **Room** (ORM فوق SQLite) + **SQLCipher** (تشفير عند الراحة، تفاصيله القسم ٨.٦:
  AES-256-CBC صفحة-بصفحة، HMAC-SHA512، مفتاح ملفوف بالـ Keystore).
- كل UI يقرأ من Room عبر **Flow** — ما ينتظر شبكة أبداً.

### ١٠.٣ التنظيف الدوري (Reaping) — حرج للـ DTN

من بحث DTN: **الذاكرة هي القيد** (مو الاتصال). مهام دورية:
- **شطب الرزم المنتهية** (`EXPIRED`، تجاوزت 24–48h أو ACKed) — يحرّر الذاكرة للحيّ.
- **تقليم seen-cache** (أقدم من `SEEN_SET_TTL`).
- **تقليم الجيران المتقادمين**.
- **حدّ أقصى للحجم:** لو امتلأت، أسقط الأقدم/الأقل أولوية — **الطوارئ تبقى دائماً**.

### ١٠.٤ واجهة وحالات اختبار

```kotlin
@Dao interface MessageDao {
    @Insert suspend fun insert(m: MessageEntity)
    @Query("UPDATE messages SET state=:s WHERE id=:id") suspend fun setState(id: String, s: Int)
    @Query("SELECT * FROM messages WHERE state IN (:active) AND expiresAt > :now")
    suspend fun dueForRebroadcast(active: List<Int>, now: Long): List<MessageEntity>
    @Query("DELETE FROM messages WHERE expiresAt <= :now OR state=:expired")
    suspend fun reap(now: Long, expired: Int): Int
    fun observe(id: String): Flow<MessageEntity?>
}
```
- [ ] كتابة/قراءة مشفّرة (فتح ملف القاعدة خام ما يكشف plaintext). — [ ] `reap`
  يشطّب المنتهي ويترك النشط. — [ ] عند الامتلاء، الطوارئ تبقى والأقدم يُسقَط.

**يحتاج بيئة أندرويد (Room/SQLCipher)؛ المنطق فوقه (آلة الحالة) قابل لاختبار JVM
بمستودع وهمي.** milestone: **M3**.

---

## ١١. نموذج البيانات وتنسيق الرزمة (Wire Format)

### ١١.١ العلاقات

```
Identity (1) ── يملك ──> TrustedContact (N)
Message  (N) ── له ────> DeliveryReceipt (0..1)
Message  (N) ── يُفهرَس ─> SeenMessageCache
AggregateRecord (N) ── يُرفَع عبر ─> Gateway → Server
```

### ١١.٢ الكيانات

- **`Identity`** (سجل واحد): `nodeId`، `signPubKey`/`signPrivEnc` (Ed25519، الخاص
  **مشفّر**)، `boxPubKey`/`boxPrivEnc` (X25519)، `displayName?`.
- **`TrustedContact`**: `nodeId`، `signPubKey`، `boxPubKey`، `displayName` (من QR)،
  `addedAt`.
- **`Message`**: `id`(MessageId)، `type`، `priority`، `originNodeId`، `destNodeId?`،
  `payload` (مشفّر للـ unicast)، `signature`، `ttlHops`، `state`، `createdAt`،
  `expiresAt`، `lastRebroadcastAt`.
- **`SeenMessageCache`**: `messageId`(PK)، `firstSeenAt` (يُكمَّل بـ rotating Bloom
  بالذاكرة).
- **`AggregateRecord`**: `category`، `axis` (مكان/محور **مو شخص**)، `payload`
  (أرقام)، `uploadState`. ⚠️ **لا حقل يعرّف فرداً بالاسم.**
- **`DeliveryReceipt`**: `messageId`، `ackedBy`، `signature`، `kind`، `receivedAt`.

البايتات الحساسة **مشفّرة عند الراحة** (SQLCipher)، والمفاتيح الخاصة **مرتين**
(Keystore فوق).

### ١١.٣ تنسيق الرزمة (مستوحى من Bitchat الحقيقي)

مدمج (BLE محدود)، قابل للتوسعة (version)، ويفصل **header مكشوف** عن **body مشفّر**.
Little-endian. (بنية مشابهة لـ Bitchat: header ثابت + TTL خارج التوقيع + حشو
أحجام ثابتة.)

```
HEADER (مكشوف):
 0  1  version      1  1  type(0=BROADCAST 1=UNICAST 2=RECEIPT)
 2  1  priority     3  1  ttlHops (ينقص كل قفزة — خارج التوقيع)
 4  16 messageId    20 16 originNodeId    36 16 destNodeId
 52 8  createdAt    60 2  bodyLen
BODY:    62  N  body (crypto_box_seal للـ unicast، أو plaintext للـ broadcast)
SIGNATURE: 62+N  64  signature (Ed25519 على header*+body)
header* = الـ header ما عدا البايت ٣ (ttlHops) يُعامَل كصفر بالتوقيع.
```

- **حشو الأحجام (padding):** نبطّن الـ body لأحجام ثابتة (٢٥٦/٥١٢/١٠٢٤/٢٠٤٨) مثل
  Bitchat — يخفي طول الرسالة ضد تحليل الحجم.
- **التجزئة (Fragment):** `packetId(16)` + `fragIndex(2)` + `fragTotal(2)` + chunk.
  أنواع `fragmentStart/Continue/End`. نجزّئ حسب **أصغر MTU متفاوَض عليه** مع الجار.
- **حالات اختبار:** encode→decode يرجّع نفس الحقول · تغيير `ttlHops` ما يكسر التوقيع
  · تغيير أي حقل آخر يكسره · `bodyLen` غلط → فشل مضبوط بلا crash · split/reassemble
  بأطوال حدّية · حشو يُزال صح عند الفك.

---

## ١٢. دروس من التطبيقات المشابهة

درسنا ٩ أنظمة حقيقية. الخلاصة: **الفكرة مجرّبة، والفشل المتكرر بالأمان والبطارية.**

### ١٢.١ جدول المقارنة

| النظام | النقل | التوجيه | التشفير | مراجعة أمنية؟ | الحالة |
|--------|------|---------|---------|---------------|--------|
| **Bitchat** (2025) | BLE (+Nostr) | فيضان، TTL=7، Bloom dedup، store-fwd | Noise XX (X25519/ChaCha20) | ❌ (انكسر — Radocea) | نشط، ينمو |
| **Briar** | BT + WiFi + Tor | مزامنة بين موثوقين (مو فيضان) | Bramble (XSalsa20-Poly1305) | ✅ Cure53 2017 | نشط |
| **FireChat** (2014) | BT + WiFi Direct | فيضان حسب الكثافة | **بلا تشفير بالأوج!** | ❌ | **ميت 2018** |
| **Bridgefy** | BLE | فيضان، بث + خاص | بلا → libsignal (لسا مكسور) | **انكسر مرتين** | SDK |
| **Meshtastic** | LoRa (مو هاتف) | managed flood، hop 3/7، SNR | AES-256 قنوات | مجتمعي | نشط قوي |
| **goTenna** | راديو مخصّص | Aspen Grove (ECHO/VINE)، TTL 3 | مشفّر (Pro) | CVE-2024-45838 | المستهلك ميت → عسكري |
| **Serval** | WiFi ad-hoc | Rhizome store-fwd | مدمج | تجريبي | متوقّف |
| **SSB** | gossip | تكرار سجلات على رسم المتابعة | Ed25519 موقّع | — | نشط (نيش) |
| **GAEN** (كورونا) | إعلان BLE فقط | بلا تمرير (إثبات قياس) | P-224 | — | أثبت القياس لمليارات |

### ١٢.٢ ما نأخذه (إيجابي)

- **Bitchat** = أقرب مرجع معماري: TTL=7، Bloom dedup، Noise، store-and-forward،
  حشو أحجام، fallback نت. **نقلّده ونصلّح اللي كسره.**
- **Meshtastic** = أفضل انضباط فيضان: **listen-before-rebroadcast + SNR-weighted
  backoff + hop limit منخفض**. **نتبنّاه.**
- **Briar** = نموذج الأمان الصح: **QR وجهاً لوجه + مراجعة Cure53 مستقلة.**
- **FireChat** = أثبت أطروحة الكثافة (١٠٠ ألف تحميل/يوم بهونغ كونغ، ذروة ٣٣ ألف
  متصل) — **والكثافة وقود مو عبء**. لكنه مات لأنه plaintext وبلا نموذج صيانة.
- **GAEN** = إثبات إن **إعلان BLE غير المتّصل يتوسّع لمليارات** (لكنه مو mesh).

### ١٢.٣ ما نتجنّبه (الدروس السلبية)

- **التبنّي يقفز بعد القطع** (Bridgefy ميانمار ١.٣ مليون/٢٤ساعة؛ Bitchat إيران
  وأوغندا) — أسوأ وقت للتثبيت. **ميزتنا: الأربعينية حدث متوقّع، فننشر التطبيق قبل.**
- **مات تطبيقات لأن السوق المستهلك غير مستدام** (FireChat، goTenna المستهلك) —
  **ميزتنا: راعٍ مؤسسي/حكومي وهدف خدمي.**
- **iOS يخنق BLE بالخلفية** (overflow area، تعليق بعد ١٠–٢٠ ثانية) — سبب إضافي
  لتركيزنا على أندرويد.

### ١٢.٤ حقيقة الجدوى بمقياس الأربعينية (صدق)

أبحاث Bluetooth Mesh وBLE تبيّن:
- **القيد ليس المدى بل التشبّع:** ٢.٤GHz عرضه ٨٣.٥MHz بس، و٣ قنوات إعلان فقط؛
  ملايين الأجهزة + Wi-Fi → تصادمات و«عاصفة بث» وانهيار goodput.
- سقف **~٤–٧ اتصالات GATT لكل تلفون**؛ موثوقية Bluetooth Mesh تنزل تحت ٨٥٪ فوق
  ~٥٠٠ عقدة بنفس القناة.
- محاكاة الحشود (الملعب): epidemic تسليم **~٠.١٨–٠.٣٨**، زمن **~٥٧٤–١٠٣٣ ثانية**.

> **الخلاصة الأمينة:** الفيضان الحيّ ينفع **محلياً (~٢٠٠م، ٧ قفزات)** —
> «وين جماعتي»، «ماء/إسعاف هنا»، SOS. أما الـ٨٠ كم **ما تُقطع بقفزات حيّة** — تحتاج
> **store-carry-forward (الناس تحمل الرسائل وهي تمشي) + بوابات/مراسي ثابتة** على
> أعمدة المسار الـ١٤٥٢ وخيام الخدمة. نصمّم على هذا الأساس، لا نوعد بمستحيل.

---

## ١٣. نموذج التهديد (Threat Model)

رسالتك تمرّ بتلفونات غرباء = **كل وسيط مهاجم محتمل**. نكون صريحين بشنو نحمي وشنو لا.

### ١٣.١ الوسيط الخبيث (يقرأ / يعدّل / يُسقِط / يعيد ترتيب / يعيد بث)

| الهجوم | الحماية |
|--------|---------|
| **يقرأ** | تشفير E2E (sealed box) → يشوف ciphertext فقط (ما يحمي الميتاداتا) |
| **يعدّل/يزوّر** | tag الـ AEAD + توقيع Ed25519 → التعديل يفشل التحقق ويُهمَل |
| **يُسقِط (blackhole)** | **ماكو تشفير يمنعه** → الحل: **تكرار/مسارات متعددة** (الفيضان أصلاً متعدد المسارات) + ACK يكشف عدم الوصول |
| **يعيد ترتيب** | أرقام تسلسل موقّعة |
| **يعيد بث (replay)** | seq + نافذة منزلقة + dedup cache (القسم ٧.٦) |

### ١٣.٢ هجوم Sybil (هوية مزيّفة كثيرة)

**Douceur 2002** أثبت: **بلا سلطة هوية مركزية موثوقة، ماكو حل لا-مركزي كامل** يمنع
مهاجم من توليد هويات لا نهائية. يضرب أي نظام يفترض «عقدة = صوت» (السمعة، المسارات
المتعددة، التصويت). تخفيف (مو حلّ كامل):
- **proof-of-work** (كلفة لكل هوية) — لكنه يحرق بطارية الهاتف، فحذرين.
- **شهادة/هوية مركزية** (الوحيد اللي يشتغل كامل، بثمن المركزية).
- **دفاعات رسم الثقة الاجتماعي** (صعوبة تزييف حواف ثقة كثيرة).

> **قرارنا:** الموثوقون (QR) محصّنون ضد Sybil (تبادل خارج النطاق). الغرباء يمرّرون
> blobs فقط، فالـ Sybil يأثّر على كفاءة التوجيه لا على سرّية المحتوى.

### ١٣.٣ الإغراق/DoS بشبكة فيضان

- **تضخيم الرسالة / TTL exhaustion / استنزاف البطارية.** تخفيف: **حدّ معدّل لكل
  عقدة** + **TTL منخفض** + **dedup** + (اختياري) **postage بـ proof-of-work** على
  المرسِل (لكن نعاير الكلفة عشان ما تصير هي نفسها استنزاف بطارية).

### ١٣.٤ تحليل المرور وتسريب الميتاداتا (اللي E2E ما يحلّه)

الـ headers (المصدر، الهدف، الوقت، الحجم) تكشف الرسم الاجتماعي حتى مع التشفير. خطر
خاص عندنا لأن الإيصالات تحتاج هوية المصدر. تخفيف (وكلفته):
- **حشو الأحجام الثابتة** (نسوّيه) — ضد تحليل الحجم.
- **تأخير عشوائي** (موجود أصلاً بالـ suppression) — يبعثر التوقيت.
- **mixing كامل (مثل Loopix)** — **غالي جداً على الهاتف** (باندويث/بطارية)، فلا
  نتبنّاه؛ نختار نقطة وسط: حشو + تأخير محدود + تقليل المعرّفات الصريحة.

> **صدق:** خصوصية الميتاداتا الكاملة (ضد خصم يراقب كل شي) **مكلفة جداً** للهاتف.
> نوضّح للمستخدم إن التطبيق يحمي **المحتوى** بقوة، لكن **القرب والتوقيت** قد يُستدَلّ
> عليهما. للحالات عالية الخطورة، هذا تطبيق مدني للخدمة مو أداة سرّية.

### ١٣.٥ ملخص: شنو يحمي التشفير وشنو لا

- ✅ **يحمي:** قراءة المحتوى، التعديل، التزوير، الانتحال (بالتوقيع + QR)، الإعادة.
- ⚠️ **ما يحمي كاملاً:** إسقاط الرزم (نخفّفه بالتكرار)، Sybil (نخفّفه بالموثوقين)،
  الميتاداتا (نخفّفه بالحشو والتأخير، لا نلغيه).

---

## ١٤. الاختبار ومحاكاة الكثافة

### ١٤.١ ما يُختبَر الآن على JVM (منطق نقي، بلا أجهزة)

| القسم | قابل JVM؟ |
|------|-----------|
| التوجيه (TTL/dedup/suppression) | ✅ كامل |
| آلة الحالة (store-and-forward) | ✅ كامل (مستودع وهمي) |
| التشفير (seal/sign/verify) | ✅ كامل (lazysodium-java) |
| wire format / fragmentation | ✅ كامل |
| انتخاب البوابة | ✅ المنطق |
| محاكاة الكثافة المنطقية | ✅ (in-memory graph) |
| BLE/الخلفية/Room/Keystore/UI | ⛔ يحتاج SDK + أجهزة |

### ١٤.٢ محاكاة الكثافة (تصميمها — لتعيير المعاملات قبل الأجهزة)

نبني **محاكي in-memory** (discrete-event أو time-stepped):
- **العقد:** N وكيل بموقع (x,y) ونموذج حركة. للحشود: نموذج كثافة عالية (٤–٥ شخص/م²)
  على ممر خطّي. نماذج حركة واقعية: SLAW / Social Force Model للحشود الكثيفة (بدل
  Random Waypoint غير الواقعي).
- **الروابط:** unit-disk graph (جيران لو المسافة ≤ R ~١٠–٣٠م)، مع احتمال فشل حسب
  المسافة.
- **نموذج MAC:** وسط مشترك، تصادم لو اثنان بالمدى أرسلوا متداخلين؛ نموذج RAD/jitter.
- **البروتوكولات:** فيضان أعمى / احتمالي(p) / counter-based(C,RAD) / DTN
  (epidemic, Spray-and-Wait L, PRoPHET).

**مقاييس نقيسها (المجموعة القياسية):**
- **Reachability (RE):** نسبة العقد اللي وصلتها الرسالة.
- **Saved Rebroadcast (SRB)** = (r−t)/r — نسبة كتم التكرار.
- **Latency** (متوسط + ذيل p95)، **التصادمات**، **الحلقات** (لازم ~٠).

**كيف نعاير:** نكنس الكثافة (نغيّر N أو R) ولكل كثافة نكنس p أو (C، RAD)، ونرسم
جبهة reachability-vs-redundancy، ونلقى **الركبة** (أصغر p/C يحافظ على RE ≥ هدف).
نتوقّع (من الأدبيات): counter-based بـ C=3 يحافظ RE≈١ ويقصّ التكرار ~٤٤٪
والتصادم ~٣٧٪.

**أدوات مرجعية:** The ONE (محاكي DTN)، ns-3، OMNeT++/INET، meshtasticator. نقدر
نكتب محاكينا الخاص بـ Kotlin/JVM للتكامل مع `Router` النقي.

### ١٤.٣ اختبارات تحتاج أجهزة (لبيئة مجهّزة)

- **تكامل BLE:** أجهزة متعددة (مو محاكي)، اكتشاف، نقل > MTU، scheduler يدوّر.
- **محاكاة كثافة فعلية:** ٥ → ١٠ → ٢٠+ جهاز لرصد العاصفة ومعايرة المعاملات.
- **صمود الخلفية:** ساعة بالخلفية + reboot، عبر OEMات (شاومي/هواوي/سامسونغ/أوبو).
- **البطارية:** قياس الاستهلاك ساعات؛ التحقق إن WakeLock ما يُمسَك إلا بمعالجة فعلية.
- **Room/SQLCipher/Keystore:** كتابة/قراءة مشفّرة، تنظيف، تغليف المفاتيح.

---

## ١٥. البيئة والحالة وخطة البناء

### ١٥.١ الحالة الحالية (الصدق أولاً)

**ماكو كود أندرويد بعد — توثيق تصميم وبحث.** البيئة السحابية الحالية ما تبني أندرويد:

| المورد | الحالة |
|--------|--------|
| Java (OpenJDK 21)، Gradle، Maven Central، GitHub | ✅ متاح |
| **Android SDK** | ⛔ غير منصَّب |
| **`dl.google.com`** (تنزيل SDK) | ⛔ محجوب |
| **`maven.google.com`** (AGP، AndroidX، Compose، Room) | ⛔ محجوب |
| أجهزة Android حقيقية | ⛔ ماكو |

**ليش حاجز:** بناء أي Android module يحتاج AGP + AndroidX (محجوب) + SDK platform
(محجوب)؛ و M1/M6 يحتاجون أجهزة متعددة.

### ١٥.٢ خطة البناء (Milestones) — الأنظمة القاتلة أول

| M | العنوان | الوحوش | يحتاج أجهزة؟ |
|---|---------|--------|--------------|
| **M1** | الخلفية + النقل | ١ + ٦ | ✅ نعم |
| **M2** | التوجيه | ٢ | 🟡 منطق JVM + تأكيد بأجهزة |
| **M3** | التخزين والتسليم | ٣ + ٧ | 🟡 منطق JVM + Room بأندرويد |
| **M4** | التشفير والهوية | ٤ | 🟡 منطق JVM + Keystore بأندرويد |
| **M5** | البوابة | ٥ | 🟡 منطق JVM + شبكة |
| **M6** | الميزات | القسم ٤ | ✅ |
| **M7** | الواجهة | Compose | ✅ |

**تسلسل الفتح الأول:** توليد المفاتيح → ملف شخصي أدنى → صلاحيات بالتسلسل (BLE →
موقع → إشعارات → إعفاء بطارية) → تشغيل mesh → تخزين مشفّر → دائرة الثقة (QR) →
مزامنة انتهازية.

**هيكل المشروع:** `app/{ui, features, crypto, mesh/{routing,forward,gateway},
transport, background, data}`.

**تقسيم Gradle (يفصل المنطق النقي عن أندرويد):**
- `:core` (JVM pure Kotlin) → routing، forward state machine، crypto engine، wire
  format، fragmenter، gateway election، **محاكي الكثافة**. ← **unit tests JVM
  تشتغل بدون SDK.**
- `:app` (Android) → transport، background، data، KeyVault، ui. ← يعتمد على `:core`.

dependencies من Maven Central لـ `:core`: `kotlin-stdlib`،
`kotlinx-coroutines-core`، `com.goterl:lazysodium-java`، `net.java.dev.jna:jna`،
`junit`/`kotlin-test`.

### ١٥.٣ المسار للأمام

1. **أصلح البيئة:** أضف `maven.google.com` و`dl.google.com` للـ allowlist وثبّت
   Android SDK → ابدأ بناء أندرويد فعلي (M1 أول). مرجع:
   https://code.claude.com/docs/en/claude-code-on-the-web
2. **ابنِ `:core` الآن:** نفّذ المنطق النقي (M2 + M4 + منطق M3/M5 + المحاكي) بـ
   JVM tests خضراء فعلية.
3. **توثيق فقط** (الوضع الحالي).

> **مبدأ التوقّف الأمين:** ما نكتب كود أندرويد ونقول "شغّال" وهو ما انبنى ولا
> انختبر. الوثائق **spec قابل للتنفيذ مباشرة** عند توفّر البيئة الصحيحة.

---

## ١٦. الافتراضات وسجل المخاطر

### ١٦.١ الافتراضات (نفترض / لو طلع غلط)

- **أغلب الزوار أندرويد** → لو لا، نحتاج نسخة آيفون (لكن iOS يخنق BLE بالخلفية).
- **عدد كافٍ يفعّلون التطبيق ويخلّونه بالخلفية** → لو القليل، الرسائل ما توصل بعيد
  → ننشر قبل الأربعينية (الحدث متوقّع = ميزتنا الكبرى).
- **يقبلون يمرّرون رسائل غرباء** → نوضّح إن المحتوى مشفّر.
- **BLE مدعوم والبطارية تصمد** → وإلا يطلعون من الشبكة / وضع موفّر قوي.
- **كثافة كافية (ناس قريبين)** → بالفراغ نخزّن ونمرّر لاحقاً (store-carry-forward).
- **النت يُلمَس أحياناً** → لو ماكو نت أبداً، تشتغل محلياً بلا مزامنة.
- **اللي يمرّر ما يقرأ** → libsodium مُختبَر.
- **السيرفر وجهة تجميع فقط** → البيانات الشخصية مشفّرة، حتى السيرفر ما يقراها.

### ١٦.٢ سجل المخاطر (مرتّب بالخطورة)

| # | الخطر | الأثر | التخفيف |
|---|------|------|---------|
| 1 | **قاتلو OEM يقتلون الخدمة** | الشبكة تتفكّك | FGS `connectedDevice` + حارس + boot + إرشاد المستخدم لكل OEM (القسم ٤) |
| 2 | **عدم تبنٍّ كافٍ** | ماكو شبكة | نشر مسبق عبر المواكب/الحسينيات (الحدث متوقّع) |
| 3 | **البطارية تنزف** (٤٨°م، ٣ أيام بلا شحن) | الناس يطفّونه | وضع موفّر، duty-cycle منخفض، RSSI-gating، عتبة طوارئ |
| 4 | **تشبّع ٢.٤GHz بالكثافة الهائلة** | goodput ينهار | suppression عدوانية، payload صغير، TTL منخفض، حدّ معدّل |
| 5 | **ثغرة أمنية تنكسر علناً** (مثل Bridgefy/Bitchat) | فقدان ثقة | libsodium، توقيع، حشو، **مراجعة مستقلة قبل الإطلاق** |
| 6 | **iOS غير مدعوم فعلياً** | نخسر مستخدمي آيفون | نركّز أندرويد؛ نوثّق القيد بصراحة |
| 7 | **توقّع غير واقعي (٨٠كم لحظي)** | خيبة أمل | نصمّم store-carry-forward + مراسي ثابتة، ونوضّح الحدود |
| 8 | **الذاكرة تمتلئ (DTN)** | تسليم ينهار | expiry + delete-ACK + حدّ حجم (الطوارئ تبقى) |

### ١٦.٣ أخطر ٣ افتراضات

لو طاحت وحدة، الفكرة بخطر: **(١) عدد كافٍ من المستخدمين** · **(٢) أندرويد ما يقتل
الخلفية** · **(٣) البطارية تصمد**.

---

## ١٧. المصطلحات والمراجع

### ١٧.١ مصطلحات

- **عقدة/Node:** تلفون مشارك له `NodeId`. **وسيط/Relay:** يمرّر بلا ما يقرأ.
  **بوابة/Gateway:** لمست نت فترفع. **قفزة/Hop:** انتقال من تلفون للي بعده.
- **Controlled flooding:** فيضان متحكّم به. **Suppression:** كتم البث بعد سماع جار.
  **TTL:** حدّ القفزات. **Seen-set:** ذاكرة الرزم المرئية (dedup).
- **DTN:** شبكة تتحمّل التأخير. **Store-carry-forward:** خزّن–احمل–مرّر.
  **Epidemic / Spray-and-Wait / PRoPHET:** خوارزميات توجيه DTN.
- **Sealed box:** تشفير لمفتاح عام بلا handshake (`crypto_box_seal`).
  **Noise XX/IK/N:** أنماط handshake. **TOFU:** ثقة أول استعمال.
- **GATT / MTU / DLE / L2CAP CoC:** مفاهيم نقل BLE. **FGS:** Foreground Service.
  **Doze:** وضع توفير أندرويد. **Bloom filter:** بنية عضوية احتمالية.

### ١٧.٢ مراجع رئيسية (مصنّفة)

**التوجيه والفيضان:**
- Ni, Tseng, Chen, Sheu — "The Broadcast Storm Problem in a MANET" (MobiCom 1999):
  https://people.eecs.berkeley.edu/~culler/cs294-f03/papers/bcast-storm.pdf
- Haas, Halpern, Li — "Gossip-Based Ad Hoc Routing" (IEEE/ACM ToN 2006):
  https://www.cs.cornell.edu/lili/liliINFOCOM02.pdf

**DTN / store-and-forward:**
- Vahdat & Becker — "Epidemic Routing": http://issg.cs.duke.edu/epidemic/epidemic.pdf
- Spyropoulos et al. — "Spray and Wait": https://chants.cs.ucsb.edu/2005/papers/paper-SpyPso.pdf
- Lindgren et al. — PRoPHET / RFC 6693: https://www.rfc-editor.org/rfc/rfc6693.html
- Burgess et al. — MaxProp (INFOCOM 2006)
- DTN Bundle Protocol: RFC 4838، RFC 5050، RFC 9171 (rfc-editor.org)

**Bloom filters:**
- Bloom filter (الصيغ): https://en.wikipedia.org/wiki/Bloom_filter
- Fan/Cao — "Summary Cache": https://pages.cs.wisc.edu/~cao/papers/summary-cache/node8.html

**نقل BLE وأندرويد:**
- Android FGS service types: https://developer.android.com/develop/background-work/services/fgs/service-types
- Android FGS timeouts (connectedDevice معفى): https://developer.android.com/develop/background-work/services/fgs/timeout
- Doze & App Standby: https://developer.android.com/training/monitoring-device-state/doze-standby
- dontkillmyapp.com (قاتلو OEM): https://dontkillmyapp.com
- BLE permissions: https://developer.android.com/develop/connectivity/bluetooth/bt-permissions
- Punch Through — Android BLE Guide (133، MTU، serialize): https://punchthrough.com/android-ble-guide/
- MTU auto-517 (Android 14): https://issuetracker.google.com/issues/288211591
- Wi-Fi Aware: https://developer.android.com/develop/connectivity/wifi/wifi-aware
- L2CAP CoC: https://developer.android.com/reference/android/bluetooth/BluetoothDevice

**التشفير والتخزين:**
- Android Keystore (لا يدعم 25519): https://developer.android.com/privacy-and-security/keystore
- منحنيات EC على أندرويد: https://www.danielhugenroth.com/posts/2021_07_ec_curves_on_android/
- SQLCipher 4: https://www.zetetic.net/blog/2018/11/30/sqlcipher-400-release/
- Noise Protocol Framework: https://noiseprotocol.org/noise.html
- Sybil Attack (Douceur 2002): https://www.microsoft.com/en-us/research/publication/the-sybil-attack/
- Loopix (mixing): https://www.usenix.org/conference/usenixsecurity17/technical-sessions/presentation/piotrowska

**التطبيقات المشابهة ودروسها:**
- Bitchat whitepaper: https://github.com/permissionlesstech/bitchat/blob/main/WHITEPAPER.md
- "Breaking Bridgefy" (CT-RSA 2021): https://eprint.iacr.org/2021/214
- "Breaking Bridgefy, again" (USENIX Security 2022): https://www.usenix.org/conference/usenixsecurity22/presentation/albrecht
- Trail of Bits — Bitchat: https://blog.trailofbits.com/2025/07/18/building-secure-messaging-is-hard-a-nuanced-take-on-the-bitchat-security-debate/
- Briar: https://briarproject.org/how-it-works/
- Meshtastic managed flood: https://meshtastic.org/blog/why-meshtastic-uses-managed-flood-routing/

**السياق (انهيار الشبكة بالتجمعات):**
- WirelessMoves — سقف اتصالات LTE: https://blog.wirelessmoves.com/2016/02/lte-and-the-number-of-simultaneously-connected-users.html
- Opensignal — أداء الشبكة بالحج 2024: https://www.opensignal.com/2024/07/17/saudi-operators-networks-remained-resilient-during-the-hajj-pilgrimage-despite-increased-demands
- حجم الأربعينية: https://en.wikipedia.org/wiki/Arba'in_pilgrimage

> **ملاحظة منهجية:** أغلب الأرقام وأسماء الـ APIs بهذا الملف مبنية على بحث عبر
> مصادر حقيقية. بعض المصادر الأكاديمية (PDFs) كانت محجوبة بالبروكسي أثناء البحث،
> فبعض الأرقام الدقيقة مأخوذة من ملخّصات البحث — تُراجَع مباشرة من المصدر الأصلي قبل
> الاعتماد النهائي عليها بقرار هندسي.

---

# الملاحق (تفاصيل تقنية موسّعة)

الملاحق تحت تجمع التفاصيل الدقيقة (أرقام، أسماء APIs، معادلات، سرد) اللي تطول لو
دخلت بصلب الأقسام. كلها مبنية على البحث بمصادر حقيقية.

---

## ملحق أ — أرقام BLE الواقعية الكاملة (لتخطيط السعة)

### أ.١ ميزانية الإعلان (Advertising) بالبايت

الإعلان القديم (Legacy) سعته **٣١ بايت**، وكل حقل يكلّف overhead:

| الحقل | الكلفة الثابتة | ملاحظة |
|------|----------------|--------|
| أي AD structure | **٢ بايت** (طول + نوع) | لكل حقل |
| Flags | **٣ بايت** | إلزامي للإعلان القابل للاتصال؛ يُحذف بالـ beacon غير المتّصل لتوفير ٣ بايت |
| Manufacturer Data | **٤ بايت** (٢ overhead + ٢ معرّف شركة) | ثم بياناتك |
| Service UUID 16-bit | ٤ بايت | للفلترة بالمسح |
| Service UUID 128-bit | **١٨ بايت** | يأكل أغلب الـ٣١! |

**ميزانية واقعية:** مع Flags (٣) + Manufacturer Data (٤) يبقى **~٢٤ بايت** لمحتواك
(`NodeId` + عدد قفزات + flags قدرة بوابة). **Scan response** يعطي ٣١ بايت إضافية.

الإعلان الممتد (Extended، BLE 5، API 26): حتى **٢٥٥ بايت** بـ PDU، **~١٦٥٠ بايت**
بالتسلسل. **فخّ شائع:** لازم `setLegacyMode(false)` وإلا يرجع يحدّك لـ ٣١ بايت بصمت.
نفحص `getLeMaximumAdvertisingDataLength()` (يرجّع ٣١ لو ماكو دعم ممتد).

> **درس Bitchat الحقيقي:** يستعمل الإعلان **القديم فقط** (مو الممتد) عشان يشتغل على
> كل الأجهزة، ويحط الهوية الحقيقية بعد اتصال GATT مو بالإعلان. **نتبنّى نفس النهج:
> الإعلان القديم أرضية، والممتد تحسين انتهازي محروس بـ `isLeExtendedAdvertisingSupported()`.**

### أ.٢ أوضاع المسح ودورة العمل (من AOSP)

| الوضع | القيمة | نافذة/فاصل | الدورة |
|------|-------|-----------|--------|
| `SCAN_MODE_OPPORTUNISTIC` | -1 | يركب على مسح غيره | سلبي |
| `SCAN_MODE_LOW_POWER` | 0 (افتراضي) | ~500ms / 5000ms | ~10% |
| `SCAN_MODE_BALANCED` | 1 | ~2000ms / 5000ms | ~40% |
| `SCAN_MODE_LOW_LATENCY` | 2 | ~5000ms / 5000ms | ~100% |

**قيود حرجة:** «٥ مرات بـ ٣٠ ثانية» للـ `startScan` (السادسة تُرفَض، API 33+
`SCAN_FAILED_SCANNING_TOO_FREQUENTLY=6`) · مسح بلا فلتر + شاشة مطفية = ممنوع ·
المسح المستمر **يُخفَّض لـ OPPORTUNISTIC بعد ~٣٠ دقيقة** (لازم ندوّره) · مسح بـ
PendingIntent يصمد عبر موت العملية لكن يحتاج فلتر.

### أ.٣ MTU والـ throughput

| المعطى | القيمة |
|--------|--------|
| MTU افتراضي | ٢٣ بايت (٢٠ مفيدة بعد ATT header ٣ بايت) |
| MTU أقصى (`requestMtu`) | ٥١٧ (→ ٥١٤ بايت مفيدة) |
| خصوصية أندرويد 14+ | يفاوض على ٥١٧ تلقائياً ويتجاهل الطلبات اللاحقة (issue #288211591) |
| طبقة الربط بلا DLE | ٢٧ بايت؛ مع DLE (BLE 4.2) حتى ٢٥١ بايت |
| throughput LE 1M (MTU افتراضي) | ~٨٥ kbps |
| throughput LE 1M + DLE + MTU كبير | ~٤٠٠–٨٠٠ kbps (تلفون ~٥٠ KB/s نموذجي) |
| throughput LE 2M + DLE + MTU 247 | ذروة ~١.٤ Mbps |
| Wi-Fi Aware (للمقارنة) | عشرات–مئات Mbps (~١٠٠–٦٠٠× BLE)، لكن طاقة ~٠.٥–٠.٧ واط |

**المهم بالترتيب:** MTU+DLE > 2M PHY > فاصل الاتصال (~١٥ms جيد) >
WRITE_NO_RESPONSE/notify فوق المؤكَّد.

### أ.٤ اتصالات GATT والخطأ ١٣٣

- سقف الاتصالات المتزامنة: **~٤–٧** (يعتمد على الشريحة؛ أجهزة رخيصة قد تقف عند ٢).
  دور-مزدوج (سيرفر+عميل) يستهلك من الجهتين → الميزانية الواقعية **~٤–٦ جيران**.
- **سقف ٣٢ عميل GATT مسجّل:** كل `connectGatt()` يسجّل واحد؛ **`close()` إلزامي**
  وإلا تتسرّب وبعد ٣٢ ما تتصل أبداً.
- **الخطأ ١٣٣ (`GATT_ERROR=0x85`):** عام، غالباً timeout. الأسباب والعلاج:
  - `autoConnect=true` أوثق من `false` ضد ١٣٣ المزمن.
  - **سَلسِل عمليات GATT** (الـ stack أحادي الخيط فعلياً) — طابور FIFO.
  - مرّر `TRANSPORT_LE` صراحةً. — استدعِ `close()` بعد كل قطع.
  - cache قديم → `refresh()` (مخفي، reflection). — أعد المحاولة بـ backoff (~٥٠٠ms+).

### أ.٥ بوابات الإصدارات (Version Gates) — أهم العتبات

| الميزة | أدنى API |
|--------|----------|
| BLE أساسي، إعلان/مسح قديم، GATT | 18–21 |
| `SCAN_MODE_OPPORTUNISTIC` | 23 |
| إعلان ممتد، 2M/Coded PHY، فحوص القدرة `isLe*Supported()` | 26 |
| Wi-Fi Aware (NAN) | 26 |
| L2CAP CoC (`createL2capChannel`) | 29 |
| `BLUETOOTH_SCAN/ADVERTISE/CONNECT` (مع `neverForLocation`) | 31 |
| `connectedDevice` FGS إلزامي النوع | 34 |
| سقف ٦ساعات على `dataSync` (لكن `connectedDevice` معفى) | 35 |

### أ.٦ هوية BLE — لا تثق بالـ MAC

أندرويد يعلن بعنوان **RPA (عشوائي خاص قابل للحلّ) يتدوّر ~كل ١٥ دقيقة**. **ما تقدر
تستعمل الـ MAC كهوية مستقرّة** — لازم تحمل معرّفك داخل الإعلان (مثل Berty يحط
libp2p PID بالـ Service Data، و GAEN يحط الـ RPI). درس مزدوج: نفس التدوير **يحمي
من التتبّع** (نتبنّاه ضد عيب Bitchat: مفاتيح ثابتة → تتبّع).

---

## ملحق ب — تفاصيل قاتلي الخلفية لكل مصنّع (OEM)

(موقع dontkillmyapp.com يرتّبهم؛ الصينيون وسامسونغ الأسوأ. ماكو ضبط برمجي — إرشاد
المستخدم فقط، مع كشف `Build.MANUFACTURER`.)

- **شاومي (MIUI/HyperOS):** تطبيق Security → **Autostart** تشغيل → موفّر البطارية
  للتطبيق = "بلا قيود" → بالـ Recents اسحب البطاقة لين يطلع قفل.
- **هواوي (EMUI):** Battery → **Launch manager** → أطفئ "الإدارة التلقائية" → فعّل
  الثلاثة: Auto-launch، Secondary launch، Run in background.
- **أوبو (ColorOS):** **Startup Manager** فعّل التطبيق + أطفئ تحسين البطارية + ثبّته
  بالـ Recents.
- **فيفو:** Battery → **Background power management** → أطفئ الإدارة التلقائية،
  فعّل التشغيل بالخلفية يدوياً.
- **ون بلس (OxygenOS):** Battery optimization = **Don't optimize** (ينتبه: **يرجّعها
  بصمت** → ثبّت بالـ Recents) + أطفئ Deep optimization.
- **سامسونغ (One UI):** Battery → **Background usage limits** → أضفه لـ **Never
  sleeping apps** + أطفئ "Put unused apps to sleep".

**سياسة Play:** صلاحية `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` مقيّدة، **لكن تطبيقنا
حالة مؤهَّلة صراحةً** (تطبيق سلامة/مراسلة لا يقدر يستعمل FCM لأن ماكو نت) — نوثّق
التبرير بـ Play Console.

**معمارية النبض (حزام + حمّالات):** FGS `connectedDevice` دائمة + حارس WorkManager
كل ١٥ دقيقة + إعادة تشغيل بـ AlarmManager عند `onTaskRemoved` + BootReceiver. كل
طبقة قابلة للفشل، فنكدّسها. **ماكو ضمان ١٠٠٪.**

---

## ملحق ج — معاملات التوجيه والـ dedup عبر الأنظمة + مثال Bloom محسوب

### ج.١ قيم TTL/hop عبر الأنظمة الحقيقية

| النظام | TTL/hop افتراضي | أقصى | عرض الحقل |
|--------|------------------|------|-----------|
| Bitchat | 7 (يحدّه ٥ بالكثافة) | 255 (حقل ٨-بت) | ٨ بت |
| Meshtastic | 3 | 7 | ٣ بت |
| goTenna (مستهلك) | 3 | ~3–4 عملياً | — |
| Bluetooth SIG Mesh | 5–7 نموذجي | 127 (٠/١=لا تمرير) | ٧ بت |
| AODV `NET_DIAMETER` | 35 | — | — |
| DTN BPv7 Hop Count | — | 255 | — |
| IP (للمقارنة) | 64–128 | 255 | ٨ بت |

**الدرس:** كل الأنظمة الواقعية للحشود تستقر على **٣–٧** لأن كلفة الفيضان تكبر بسرعة.

### ج.٢ تحليل تغطية Ni et al. (الأساس النظري)

- أقصى تغطية إضافية لإعادة بث واحدة: **~٦١٪** (عند الحافة، d→r).
- المتوقّعة: **~٤١٪** (توزيع منتظم).
- EAC(k): بعد سماعها مرتين ~**١٩.٧٪**؛ بعد ٣–٤ مرات **تحت ~٥٪** → عتبة `C=3`.
- مكاسب counter-based: قصّ التكرار **~٤٤٪**، التصادم **~٣٧٪**، RE≈فيضان عند C≥3.

### ج.٣ نظرية Gossip (Haas) — القيم

- `GOSSIP1(p)`: بثّ باحتمال p. عتبة نفاذية `p₀`؛ سلوك ثنائي.
- `p ≈ 0.65–0.75` يكفي لوصول شبه كامل، يوفّر **~٣٥٪**.
- `GOSSIP1(p,k)`: احتمال ١ لأول `k≈4` قفزات ثم p (يمنع الموت قرب المصدر).
- `GOSSIP3(0.65,4,1)` = **٦٧٪** من رسائل الفيضان (مقابل ٧٥٪ لـ GOSSIP1(0.75,4)).
- **الكثافة:** أكثف = `p₀` أقل = p أصغر. بالأربعينية نقدر نستعمل p منخفض + suppression
  عدوانية.

### ج.٤ مثال Bloom filter محسوب (للـ seen-set كمُسرّع)

نريد seen-set لـ **n=10,000 معرّف** بنسبة إيجابي كاذب **١٪**:
- البتات: `m = −n·ln(p)/(ln2)² = −10000·(−4.605)/0.4805 ≈ 95,851 بت ≈ 11.7 KB`.
- الهاشات: `k = (m/n)·ln2 ≈ 9.585·0.693 ≈ 6.64 → 7`.
- التحقق: `p_FP = (1/2)^7 ≈ 0.78٪` < ١٪. ✅
- القاعدة: **~٩.٦ بت/عنصر لـ ١٪، ~١٤.٤ بت/عنصر لـ ٠.١٪**.

> **تذكير حاسم (القسم ٦.٣):** Bloom **ما يصلح كسلطة وحيدة** لقرار «شفتها» (إيجابي
> كاذب = إسقاط رسالة جديدة بصمت = خطر). نستعمله **كمُسرّع** أمام مجموعة ID **دقيقة
> (LRU بالزمن)**. للنوافذ المنزلقة: **rotating Bloom** (فلترين نتبادلهما)، والنافذة
> لازم **أطول من أقصى عمر رسالة** وإلا ننسى ID حيّ ونعيد تضخيمه.

---

## ملحق د — خوارزميات DTN بالتفصيل والمعادلات

### د.١ Epidemic (Vahdat-Becker)

كل عقدة تمسك buffer + **summary vector** (قائمة مدمجة بمعرّفات ما عندها، غالباً
Bloom). عند اللقاء: **anti-entropy session** — تتبادلان الـ vectors، كل وحدة تطلب
الناقص. تسليم أقصى، **هدر هائل**. حقول الرزمة: معرّف فريد + hop count + طلب ACK
اختياري. القيد: **الذاكرة** (FIFO/drop-oldest عند الامتلاء).

### د.٢ Spray-and-Wait (Spyropoulos)

- **Spray:** المصدر يوزّع **L نسخة**. **Binary:** عقدة عندها n نسخة تعطي ⌊n/2⌋
  لعقدة جديدة وتحتفظ ⌈n/2⌉، حتى تصير نسخة وحدة.
- **Wait:** نسخة وحدة → تسليم مباشر فقط (تحملها للهدف).
- إجمالي الإرسال **≤ L−1** (مقابل اللانهائي بـ epidemic). `L ≈ 20–25٪` من عدد العقد
  سويت-سبوت بالحشود. **Spray-and-Focus:** النسخة الأخيرة تُمرَّر لعقدة أعلى utility
  (آخر لقاء بالهدف) بدل الانتظار السلبي.

### د.٣ PRoPHET (Lindgren) — المعادلات والثوابت

احتمال تسليم `P(a,b) ∈ [0,1]`:
1. **عند اللقاء:** `P(a,b) = P_old + (1−P_old)·P_init` — `P_init = 0.75`.
2. **الشيخوخة:** `P(a,b) = P_old · γ^K` — `γ = 0.98`.
3. **التعدّي:** `P(a,c) = P_old + (1−P_old)·P(a,b)·P(b,c)·β` — `β = 0.25`.

التمرير: أعطِ الرزمة لعقدة `P(·,d)` أعلى منك. (RFC 6693/PRoPHETv2 يرفع γ/β لـ
~٠.٩٩٩/٠.٩.)

### د.٤ MaxProp (Burgess) — الترتيب الخماسي

احتمال تسليم متزايد منظّم لمتجه احتمالات، وكلفة المسار `Σ(1−likelihood)`. ترتيب
الإرسال/الإسقاط على كل لقاء:
1. رزم موجّهة للعقدة الملتقى بها.
2. معلومات التوجيه.
3. **الإيصالات (ACKs)**.
4. رزم منخفضة عدد القفزات (head-start للجديد).
5. الباقي حسب الكلفة (الأقل أولاً).

**ACK يُفاض شبكياً** → كل عقدة تشوفه **تحذف الرزمة المؤكَّدة** (تنظيف ذاكرة) +
**قائمة الوسطاء السابقين** بكل رزمة (منع الحلقات).

### د.٥ Bundle Protocol (DTN)

- RFC 5050 (BPv6): bundles، store-and-forward، **custody transfer** (نقل مسؤولية
  التسليم بين custodians).
- RFC 9171 (BPv7، ٢٠٢٢): ترميز **CBOR**، primary block غير قابل للتعديل، custody
  صار طبقة امتداد. **Bundle Age Block** (عمر بالملّي ثانية للعقد بلا ساعة) +
  **Hop Count Block** (حدّ ١–٢٥٥). **lifetime** يحدّ التخزين.

### د.٦ جدوى الحشود (أرقام محاكاة)

- الكثافة العالية (٤–٥ شخص/م²) = **التقاءات كثيرة** → نموذج النسخ يزدهر.
- **القيد = الذاكرة:** epidemic بـ buffer ٥MB تسليم ~**٠.٢٠**، بـ ٢٠MB ~**٠.٣٨**
  (٦٠–٨٠٪ يضيع!). الزمن ~**٥٧٤–١٠٣٣ ثانية**.
- **delete-ACK إلزامي** لتحرير الذاكرة. **Spray-and-Wait يتفوّق على epidemic تحت
  ذاكرة محدودة.**

---

## ملحق هـ — أنماط Noise وأحجام libsodium

### هـ.١ أحجام libsodium

| العنصر | الحجم |
|--------|------|
| Ed25519 مفتاح عام / توقيع | ٣٢ / ٦٤ بايت |
| X25519 مفتاح | ٣٢ بايت |
| `crypto_box_seal` overhead | ٤٨ بايت (مفتاح عابر ٣٢ + MAC ١٦) |
| ChaCha20-Poly1305 tag | ١٦ بايت |

### هـ.٢ أنماط Noise (الحرف الأول=مفتاح البادئ، الثاني=المستجيب)

`N`=بلا مفتاح ثابت · `K`=معروف مسبقاً · `X`=يُرسَل مشفّراً أثناء · `I`=فوراً.

- **XX:** الطرفان ما يعرفان مفاتيح بعض مسبقاً، يتبادلانها مشفّرة (٣ رسائل، يحتاج
  تزامن) — اختاره libp2p. **يستعمله Bitchat** (`Noise_XX_25519_ChaChaPoly_SHA256`).
- **IK:** البادئ يعرف مفتاح المستجيب، يرسل مفتاحه فوراً (0-RTT).
- **N (أحادي):** مرسِل مجهول لمفتاح معروف = **بالضبط sealed box** — رسالة وحدة
  مكتفية، بلا تزامن. **هذا اختيارنا للـ mesh اللاتزامني** (القسم ٨.٣).

> **ليش XX التفاعلي لا يصلح لنا:** يحتاج الطرفين متّصلين بنفس اللحظة لإكمال ٣ رسائل.
> بعقد متحركة تختفي ورسائل تُخزَّن-تُمرَّر عبر غرباء، **مستحيل**. النمط `N`/sealed box
> يحلّ هذا.

### هـ.٣ SQLCipher 4 (PRAGMAs)

- AES-256-CBC صفحة-بصفحة + HMAC-SHA512، salt ١٦ بايت بأول الملف، صفحة ٤٠٩٦.
- KDF: PBKDF2-HMAC-SHA512 بـ **٢٥٦,٠٠٠ تكرار**.
- **raw key (تخطّي KDF):** `PRAGMA key = "x'<64 hex>'"` (٣٢ بايت مفتاح) — نستعمله
  لأن مفتاح DB الملفوف بالـ Keystore عالي الإنتروبيا أصلاً.
- المكتبة: `net.zetetic:sqlcipher-android` (`SupportOpenHelperFactory`). أداء
  ~٥–١٥٪ overhead.

---

## ملحق و — سرد تفصيلي للتطبيقات المشابهة (مع أرقام)

### و.١ Bitchat (Jack Dorsey، يوليو 2025) — أقرب مرجع

- **النقل:** BLE mesh + fallback **Nostr** (٣ طبقات: BLE مباشر → Nostr → طابور).
- **التوجيه:** فيضان، **TTL=7** (يحدّه ٥ بالكثافة)، **Bloom dedup**، split-horizon،
  store-and-forward للعقد غير المتّصلة.
- **الرزمة:** header ١٣ بايت (version، type، TTL، timestamp ٨، flags، payload len
  ٢)، sender ٨، recipient ٨ اختياري، signature ٦٤، **حشو ٢٥٦/٥١٢/١٠٢٤/٢٠٤٨**.
- **UUIDs الحقيقية (من الكود):** service `F47B5E2D-...`, characteristic `A1B2C3D4-...`,
  CCCD `0x2902`. دور-مزدوج (server + client). MTU 517 بعد ~٢٠٠ms من اكتشاف الخدمات.
- **التشفير:** `Noise_XX_25519_ChaChaPoly_SHA256`.
- **الحجم:** ~١٠ آلاف بأول ٤٨ ساعة؛ ~٣٦٠ ألف بسبتمبر؛ ~٧٠ ألف/أسبوع بمدغشقر.
- **الثغرات:** أُطلق بلا مراجعة → **MITM/انتحال (Radocea)** بسبب مصادقة مكسورة؛
  مفاتيح ثابتة → **تتبّع BLE**؛ Trail of Bits: «لا تعتمد عليه لاتصالات حسّاسة».

### و.٢ Briar — نموذج الأمان الصح

- **النقل:** Tor (للنت) + Bluetooth RFCOMM (مو BLE!) + Wi-Fi + USB.
- **التوجيه:** **مزامنة بين موثوقين فقط** (مو فيضان للغرباء)، delay-tolerant.
- **التشفير:** Bramble (BTP: XSalsa20-Poly1305 + مفاتيح متدوّرة؛ BSP للمزامنة؛ BHP/
  BQP/BRP للثقة). **QR وجهاً لوجه** يمنع MITM.
- **مراجعة:** **Cure53 (2017)** — «جودة كود استثنائية». Briar Mailbox (2023) للتسليم
  اللاتزامني.
- **الحجم:** +١ مليون تثبيت Play (~٤.٢ مليون تحميل). تمويل OTF/NLnet.
- **القيد لنا:** مو فيضان للغرباء — ممتاز للأمان مو لـ «٢٢ مليون غريب يمرّرون».

### و.٣ Bridgefy — الكارثة الأمنية (انكسر مرتين)

- **الحجم:** هونغ كونغ 2019 (+٤٠٠٠٪)؛ **ميانمار ١.٣ مليون/٢٤ساعة**؛ إيران +١٢ مليون.
- **انكسار ١ (2020):** بلا مصادقة → انتحال؛ معرّفات صريحة → رسم اجتماعي؛ **RSA
  PKCS#1 v1.5** (Bleichenbacher، ~١٤ ساعة لفكّ)؛ **ضغط ثم AES-ECB** → CRIME؛ **zip
  bomb** (١٠KB → ١٠MB) يطفّي الشبكة كلها.
- **انكسار ٢ (2022، بعد تبنّي libsignal):** **TOCTOU** يكسر السرّية ~٥٠٪؛ استرجاع
  البثّ بلا المفتاح؛ MITM وانتحال البثّ باقيان.
- **الدرس:** «تبنّينا Signal» ≠ آمن لو التركيب غلط.

### و.٤ FireChat (2014) — أثبت الكثافة، مات بلا أمان

- **النقل:** BT + Wi-Fi Direct + Multipeer. **«كل ما كبر الحشد، اشتغل أحسن».**
- **الحجم:** هونغ كونغ ١٠٠ ألف/يوم، **ذروة ٣٣ ألف متصل**، ٥٠٠ ألف بأسبوعين، ١٠.٢
  مليون جلسة؛ العراق ٤٠ ألف/عطلة (2014).
- **الفشل:** **بلا تشفير إطلاقاً** بالأوج (غرف عامة plaintext)، E2E أُضيف ٢٠١٥ متأخراً.
- **مات ٢٠١٨** (Open Garden تحوّلت لكريبتو). **درس استدامة:** حتى الفيروسي يموت بلا
  نموذج صيانة.

### و.٥ Meshtastic — أفضل انضباط فيضان (LoRa)

- **النقل:** **LoRa** (راديو مخصّص، مو هاتف؛ BLE بس يربط الهاتف بالراديو).
- **managed flood:** hop_limit **٣/٧**، **listen-before-rebroadcast**، **SNR-weighted
  CW** (البعيد يبثّ أول، CWmin=2/CWmax=7، `delay=2·CWmax+rand(0,2^CW)`).
- **القيد:** airtime/duty-cycle (EU 10٪)؛ يتدهور فوق ~١٠٠ عقدة/قناة. **هواة/إقليمي
  مو مدني-حجم.** لكن **خوارزمية الفيضان مرجعنا الأول.**

### و.٦ goTenna — راديو مخصّص، تحوّل عسكري

- **Aspen Grove** (بلا حزم تحكّم، ECHO للبثّ + VINE للـ unicast)، TTL=3.
- **ECHO:** يقصّ الإرسال ~٣× ويزيد البطارية >٥٠٪. **المستهلك توقّف ٢٠٢٣** → عسكري
  (Pro X). **CVE-2024-45838** (مولّد عشوائي ضعيف). **درس:** السوق المستهلك غير مستدام.

### و.٧ Serval / SSB / GAEN / AirDrop (مختصر)

- **Serval:** Wi-Fi ad-hoc + Rhizome store-forward؛ نشأ من زلزال هايتي ٢٠١٠؛ عطّله
  شرط الـ root وقلّة التمويل — تجريبي.
- **SSB:** سجلات append-only موقّعة (Ed25519)، gossip على رسم المتابعة، sneakernet؛
  اتساق نهائي مو لحظي.
- **GAEN (كورونا):** **إعلان BLE غير متّصل (لا تمرير)** — أثبت **القياس لمليارات
  جهاز**؛ RPI يتدوّر ١٠–١٥ دقيقة + MAC عشوائي متزامن (درس الخصوصية). **مو mesh** لكنه
  دليل قياس الإعلان.
- **AirDrop / Find My:** BLE اكتشاف + AWDL/Wi-Fi نقل. **درس خصوصية:** AirDrop يسرّب
  أرقام/إيميلات عبر هاش SHA-256 ضعيف (PrivateDrop؛ والصين كسرته ٢٠٢٤). Find My =
  relay مواقع مشفّرة E2E عبر مليار جهاز (لكنه تتبّع أغراض مو مراسلة).

---

## ملحق ز — انهيار الشبكة الخلوية بالتجمعات (السياق بالأرقام)

### ز.١ ليش تنهار (٤ أسقف)

1. **موارد الراديو (PRBs)** محدودة. 2. **سقف المتصلين (RRC):** ~**٩٠٠/خلية** (اختبار
WirelessMoves: ٤٠٠٠ جهاز على خليتين = ١٨٠٠ اتصال). 3. **قناة التحكّم (PDCCH):**
~١٦ مستخدم/TTI واقعياً. 4. **عاصفة الإشارات (RACH):** فشل الاتصالات عند الذروات
المتزامنة. نتيجة: نسبة فشل **~٤٠٠×** بالفعاليات المزدحمة.

### ز.٢ أرقام التجمعات

| الحدث | الحجم | البنية المضافة |
|------|-------|----------------|
| الأربعينية 2023–2025 | ~٢٢ مليون (أكبر تجمّع سنوي) | نت مجاني بكربلاء، مضاعفة سعة الحدود |
| الحج 2024 | ~٢ مليون | ٤٤.٨ مليون مكالمة، ٥.٧٩ بيتابايت، ٦٢٠٠+ برج (٤٠٠٠ 5G) |
| كومب ميلا 2025 | +٦٦٠ مليون (ذروة ~٨٠ مليون/يوم) | ٣٢٨ برج + ٧٨ متنقّل + ١٥٠ خلية صغيرة |

> المسير: **~٨٠ كم، ١٤٥٢ عمود (٥٠م)، ٣ أيام، حتى ٤٨°م**. كثافة ٤–٥ شخص/م² (>٥ =
> خطر تدافع). **الحل التقليدي = ضخّ أبراج مؤقتة** (غالٍ، ما يغطّي ٨٠كم). **البديل:
> تلفونات الناس نفسها.**

---

## ملحق ح — قائمة فحص التنفيذ (Checklist مكثّفة)

**M1 (خلفية + نقل، يحتاج أجهزة):**
- [ ] FGS `connectedDevice` + START_STICKY + إشعار فوري + POST_NOTIFICATIONS.
- [ ] BootReceiver + حارس WorkManager + AlarmManager (onTaskRemoved).
- [ ] شاشة إرشاد OEM حسب `Build.MANUFACTURER` + فحص `isIgnoringBatteryOptimizations` دوري.
- [ ] إعلان قديم (beacon ٢٤ بايت) + مسح بفلتر Service UUID + duty-cycle حسب البطارية.
- [ ] GATT دور-مزدوج، خاصية write+notify، CCCD، MTU 517، طابور FIFO، `close()` دائماً.
- [ ] Fragmenter (split/reassemble) — **JVM-testable**.

**M2 (توجيه، منطق JVM):**
- [ ] seen-set دقيق (LRU) + Bloom مسرّع اختياري. — [ ] TTL + suppression (counter
  C=3، RAD SNR-weighted). — [ ] طابور طوارئ منفصل. — [ ] محاكي كثافة + مقاييس RE/SRB.

**M3 (تسليم + تخزين):**
- [ ] آلة الحالة + receipts موقّعة + delete-ACK. — [ ] Spray-and-Wait (L≈8). — [ ]
  expiry + reaping + حدّ حجم (الطوارئ تبقى). — [ ] Room + SQLCipher (raw key).

**M4 (تشفير):**
- [ ] libsodium identity (Ed25519 + X25519). — [ ] sealed box + sign/verify. — [ ]
  Keystore AES-GCM wrap (StrongBox→TEE fallback). — [ ] QR trust + TOFU.

**M5 (بوابة):**
- [ ] gossip election + idempotent upload + جدول «ماذا يُرفع» الملزم + pinning.

**M6–M7 (ميزات + واجهة):**
- [ ] ميزات تحترم الخطوط الحمراء. — [ ] مؤشرات ⏳/✓/✓✓. — [ ] زر SOS بشري.

> **قبل الإطلاق (إلزامي):** **مراجعة أمنية مستقلة** (درس Bitchat/Bridgefy) + اختبار
> كثافة فعلي ٢٠+ جهاز + اختبار صمود خلفية عبر OEMات + قياس بطارية تحت حرارة عالية.

---

## ملحق ط — طبقة الميزات بالتفصيل (فوق البنية)

الميزات تركب **فوق** الوحوش السبعة. كلها أوفلاين أولاً، وكلها تُفحَص ضد الخطوط
الحمراء **قبل** التنفيذ. القاعدة: رقم عن **مكان/خدمة/موكب** ✅، عن **إنسان فرد** ⛔،
ويُجمَع **المتاح** (بإرادة صاحبه) لا يُنتزَع بالمراقبة.

### ط.١ الميزات الشخصية (E2E، للموثوقين)

| الميزة | كيف تشتغل | الوحوش المستعملة | فحص الخط الأحمر |
|--------|-----------|-------------------|------------------|
| **الموقع** | بثّ موقعك **لموثوقيك فقط** (unicast مشفّر بالاسم) | ٤ (seal) + ٢ (unicast) + ٣ (تسليم) | ✅ المستلم موثوق اخترته؛ السيرفر ما يقرأ blob |
| **الرسائل** | قصيرة للموثوقين مباشرة؛ بعيدة عبر relay | ٢+٣+٤ | ✅ E2E، الوسطاء يمرّرون blob |
| **المبيت** | صاحب نزل يبثّ broadcast «مكان لـ X + موقعي»؛ الباحث يتطابق | ٢ (broadcast) | ✅ عرض بإرادة العارض، رقم عن مكان |

مؤشرات الحالة ⏳/✓/✓✓ من آلة حالة وحش ٣. فصل خيارات العرض (عوائل/رجال) **اختيار
بإرادة العارض، مو تصنيف مفروض**.

### ط.٢ الميزات التجميعية (مكشوفة، عن مكان/خدمة — لا فرد)

| الميزة | المحتوى | فحص الخط الأحمر |
|--------|---------|------------------|
| **بيانات الموكب** | وجبات، مخزون، تواليتات (موقع/عدد/زحمة)، ماء، إسعاف، أماكن نوم | ✅ عن موكب/مكان |
| **الصحة العامة** | عدد الحالات وأنواع الأمراض **بكل محور** → إنذار تفشّي مبكّر | ✅ **أرقام عن محور، مو سجل فرد** |
| **حركة المواكب** | جدول: متى يتحرك كل موكب ومن وين | ✅ عن موكب |
| **الموردون** | دليل محلات الجملة (موقع/بضاعة/سعر/توفّر) | ✅ عن محل |
| **مالية المواكب** | دعم مستلم، صرف، طاقة الخدمة — **شفافية كيان خدمي** | ✅ كيان مو فرد |
| **الكثافة/العدد** | عدد بكل محور، خرائط ازدحام لتجنّب التدافع | ✅ تجميعي |

هذي ترتفع للسيرفر **مكشوفة** عبر البوابة (وحش ٥، جدول «ماذا يُرفع»).

### ط.٣ السلامة (بشري — مو آلي)

- **زر استغاثة (SOS):** رزمة طوارئ (TTL أعلى، طابور منفصل، أولوية) توصّل **لجهة
  إنسان مسؤول**. **بشري بالكامل — ماكو قرار آلي، ماكو حرمان آلي.**
- **بلاغ خطر بمكان:** ازدحام/حادث **بمكان** (تجميعي) لتنبيه الناس وتوجيه المساعدة.

### ط.٤ القاعدة الحاكمة

> التطبيق **ييسّر** معلومة وإرشاد وخيارات وبلاغ طوارئ بشري — **لا يراقب ولا يعاقب**.
> أي ميزة تخالف هذا **تُرفَض**، حتى لو طُلبت لاحقاً، ويُشار للخطوط الحمراء. ولأن
> بيانات الأفراد **E2E** (حتى السيرفر ما يقراها)، التصميم نفسه **يمنع** بناء ملفات
> تتبّع — مو مجرّد سياسة، بل **قيد معماري**.

---

## ملحق ي — قرارات تصميم مفتوحة (تُحسَم عند التنفيذ)

أمانة: فيه قرارات ما نقدر نحسمها نظرياً — تحتاج تجربة بأجهزة فعلية أو معايرة. نسجّلها
عشان ما تُنسى:

1. **معاملات التوجيه الدقيقة** (`DEFAULT_TTL`، `SUPPRESS_THRESHOLD`، نافذة RAD،
   `GOSSIP_P`): القيم الابتدائية مبنية على الأدبيات، لكن **تُعاير بمحاكاة الكثافة ثم
   بأجهزة فعلية** (٥→١٠→٢٠+). الكثافة الهائلة بالأربعينية قد تتطلّب قيم أعدوانية.
2. **L لـ Spray-and-Wait:** بدأنا بـ ~٨ / ~٢٠٪ عقد، لكن العدد الفعلي يعتمد على
   تقدير الكثافة المحلية الحيّة — قد نحتاج **L تكيّفي**.
3. **حجم الـ seen-set ونافذته الزمنية:** لازم النافذة > أقصى عمر رسالة؛ التوازن بين
   الذاكرة والنسيان يُعاير على أرقام حقيقية.
4. **عتبة التصعيد لـ Wi-Fi Aware:** متى نرفع من BLE لـ Wi-Fi (حجم الحمولة، استقرار
   الجار، مستوى البطارية) — يحتاج تجربة طاقة فعلية.
5. **مراسي ثابتة (Anchors)؟** هل نضيف **عقد relay ثابتة مدعومة** على أعمدة المسار
   الـ١٤٥٢ وخيام الخدمة لجسر المقاطع الطويلة؟ هذا يرفع الموثوقية كثيراً لكنه يضيف
   بنية تحتية (قرار مع الجهة المنظِّمة).
6. **iOS:** هل نبني نسخة آيفون رغم خنق BLE بالخلفية (overflow area، تعليق ١٠–٢٠
   ثانية)؟ أو نقبل أندرويد-فقط؟ قرار منتج.
7. **سياسة الحشو (padding):** الأحجام الثابتة (٢٥٦/٥١٢/...) تحمي من تحليل الحجم لكن
   تزيد الباندويث — نوازن حسب حساسية الميتاداتا المقبولة.
8. **proof-of-work ضد الإغراق:** يحمي لكن يحرق بطارية — نعاير الكلفة أو نتركه.

> هذي **مو نواقص بالتصميم** — هي **نقاط معايرة صريحة**. حسمها بالتخمين النظري خطأ؛
> حسمها بالقياس هو الصح. (مبدأ التوقّف الأمين: نقول «يُعاير» بدل ما ندّعي رقم نهائي.)

---

## خاتمة

هذا الملف **بحث وتصميم**، مو كود. غطّينا: المشكلة بالأرقام، الفكرة، الستاك،
المعمارية، الأنظمة السبعة بعمق (مع APIs وأرقام حقيقية)، نموذج البيانات، تنسيق
الرزمة، دروس ٩ تطبيقات مشابهة، نموذج التهديد، الاختبار والمحاكاة، خطة البناء،
الافتراضات، سجل المخاطر، والملاحق التفصيلية.

**الخلاصة الأمينة:** الفكرة **مجرّبة وممكنة محلياً** (تنسيق، تشفير، تسليم — منطق نقي
قابل للاختبار الآن)، لكن **التحدّيات الحقيقية** (صمود الخلفية ضد OEM، البطارية تحت
الحرارة، تشبّع ٢.٤GHz بالكثافة الهائلة، التبنّي الكافي) تحتاج **أجهزة فعلية ومعايرة
ميدانية** — ولذلك نبني `:core` المنطقي أولاً، ونؤجّل قشرة أندرويد لبيئة مجهّزة، ولا
ندّعي «شغّال» قبل ما يُبنى ويُختبَر.

القصة المبسّطة للفكرة بملف **[القصة](القصة.md)**.

---

## ملحق ك — مثال ملموس: ما يُبنى ويُختبَر الآن (`:core`)

عشان نثبت إن الكلام مو نظري بحت، هذا هيكل وحدة `:core` (Kotlin/JVM نقي، بلا أندرويد)
اللي **تُبنى وتُختبَر فعلاً بالبيئة الحالية عبر Maven Central** (`kotlin-stdlib`،
`kotlinx-coroutines-core`، `lazysodium-java`، `junit`):

```
core/src/main/kotlin/
 ├─ wire/        WireFormat.kt  Fragmenter.kt        (ترميز/فك + تجزئة)
 ├─ routing/     Router.kt  SeenSet.kt  Suppressor.kt (TTL + dedup + suppression)
 ├─ forward/     ForwardingStore.kt  StateMachine.kt  (آلة الحالة + receipts)
 ├─ crypto/      CryptoEngine.kt                       (sealed box + sign/verify)
 ├─ gateway/     GatewayElection.kt                    (انتخاب score)
 └─ sim/         MeshSimulator.kt  UnitDiskGraph.kt    (محاكي الكثافة)
core/src/test/kotlin/  ... اختبارات JUnit لكل ما فوق
```

**أمثلة اختبارات تشتغل الآن (لا تحتاج جهاز):**

```kotlin
// التوجيه
@Test fun seenPacketIsDropped() { ... assertEquals(Drop, router.onPacketReceived(p, peer)) }
@Test fun ttlOneDeliveredNotRebroadcast() { ... }      // ttl=1 → DeliverLocal فقط
@Test fun emergencyBypassesSuppression() { ... }

// تنسيق الرزمة
@Test fun encodeDecodeRoundTrips() { assertEquals(p, decode(encode(p))) }
@Test fun changingTtlKeepsSignatureValid() { ... }      // ttlHops محسوب كصفر
@Test fun tamperingBodyBreaksSignature() { assertFalse(verify(...)) }
@Test fun fragmentReassembleRoundTrips() { assertEquals(p, reassemble(split(p, 180))) }

// التشفير (عبر lazysodium-java على JVM)
@Test fun thirdPartyCannotUnseal() { assertNull(unseal(seal(m, recv.pub), attacker)) }
@Test fun forgedSignatureRejected() { assertFalse(verify(m, sign(m, B), A.pub)) }

// آلة الحالة
@Test fun matchingReceiptEscalatesToAcked() { ... }     // ويتوقف الانتشار + يحذف
@Test fun expiredAfter48hReaped() { ... }

// محاكاة الكثافة (تعيير قبل الأجهزة)
@Test fun fiveHopChainDeliversNoLoops() { val sim = MeshSimulator(nodes=50); ... }
@Test fun counterC3KeepsReachabilityNearFlooding() { ... assertTrue(re >= 0.99) }
```

**المحاكي (`MeshSimulator`)** يبني unit-disk graph لـ N عقدة متحركة، يمرّر رزم عبر
`Router` النقي، ويقيس **RE / SRB / latency / loops** عبر كنس الكثافة — فنعاير
`SUPPRESS_THRESHOLD` و `GOSSIP_P` و TTL **قبل** ما نلمس أي جهاز.

> هذا هو **الخيار ٢** بالقسم ١٥.٣: نبني القلب المنطقي باختبارات خضراء فعلية الآن،
> ونؤجّل قشرة أندرويد (BLE/خلفية/Room/Keystore/UI) لبيئة فيها SDK وأجهزة. لو تبي،
> أقدر أبدأ أكتب هذا الـ `:core` فعلياً كخطوة جاية.
