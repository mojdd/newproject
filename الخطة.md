# الخطة — التصميم التقني لشبكة الأربعينية 🛠️

> هذا ملف **الخطة**: التصميم، الأنظمة الصعبة ("الوحوش")، نموذج البيانات، تنسيق
> الرزمة، خطة البناء، الافتراضات، وحالة البيئة. الفكرة والقصة بملف
> **[القصة](القصة.md)**.

**المبدأ الحاكم لكل شي: أوفلاين أولاً.** ولا طبقة تتوقف بسبب غياب النت. والسيرفر
**وجهة مزامنة انتهازية فقط** — يصرّف المخزون لمن يتوفّر نت، لا غير.

---

## ١. الستاك (التقنيات) ومبرّراته

| الطبقة | الاختيار | السبب |
|--------|----------|-------|
| المنصّة | **Android native** | الجمهور ~كله أندرويد؛ BLE + الخلفية يحتاجون تحكّم native |
| اللغة | **Kotlin** | لغة أندرويد الرسمية |
| الواجهة | **Jetpack Compose** | واجهات حديثة، بناء سريع |
| التزامن | **Coroutines + Flow** | إدارة شبكة/DB/حالات غير متزامنة |
| المدى القصير | **BLE (GATT)** | استهلاك بطارية منخفض، أساس الـ mesh المستمر |
| الباندويث الأعلى | **Wi-Fi Aware / Direct** | نقل بيانات أكبر؛ الدعم يختلف بين الأجهزة |
| التشفير | **libsodium** (`lazysodium`) | E2E مُختبَر؛ sealed boxes تناسب mesh لا-تزامني |
| التخزين | **Room + SQLCipher** | قاعدة محلية مشفّرة |
| الخلفية | **Foreground Service** (`connectedDevice`) | يبقي التلفون عقدة relay بالجيب |

---

## ٢. المعمارية الطبقية

```
        تلفون زائر (عقدة mesh)
   ┌──────────────────────────────┐
   │  UI (Compose)                │
   ├──────────────────────────────┤
   │  Features (موقع، رسائل، مبيت…)│
   ├──────────────────────────────┤
   │  Crypto / Identity           │  ← وحش ٤
   ├──────────────────────────────┤
   │  Mesh: routing + forward     │  ← وحش ٢،٣
   ├──────────────────────────────┤
   │  Transport: BLE + Wi-Fi Aware│  ← وحش ٦
   ├──────────────────────────────┤
   │  Background survival         │  ← وحش ١
   ├──────────────────────────────┤
   │  Local storage               │  ← وحش ٧
   └──────────────────────────────┘
            ▲ لمن تتوفّر نت → بوابة (وحش ٥)
            ▼
     ☁️ سيرفر حكومي (تجميع + تمرير blobs مشفّرة)
```

**مصطلحات:** *عقدة* = تلفون مشارك له `NodeId` من مفتاحه العام · *وسيط (relay)* =
عقدة تمرّر رزمة مو لها (تشوف blob مشفّر فقط) · *بوابة* = عقدة لمست نت فترفع المسموح
· *موثوق* = بادلته مفتاحك وجهاً لوجه (QR) · *غريب* = relay مجهول يمرّر بلا ما يقرأ.

---

## ٣. الأنظمة الصعبة ("الوحوش") السبعة

قلب التطبيق سبعة أنظمة، لو فشل أول وحشين التطبيق ميت. ترتيب البناء مقصود.

### 🪨 وحش ١ — البقاء حي بالخلفية
- **المشكلة:** أندرويد يقتل الخلفية ليوفّر بطارية، وكل مصنّع (OEM) يقتل بطريقة مختلفة.
- **الحل:** Foreground Service نوع `connectedDevice` + إشعار دائم خفيف +
  `START_STICKY` + إعادة تشغيل بعد reboot (`BootReceiver`) + جدولة مسح متقطّعة ذكية
  ضد Doze + طلب إعفاء البطارية + **شاشة إرشاد تكيّفية حسب المصنّع** + heartbeat
  لكشف القتل وإعادة التشغيل.
- **WakeLock فقط أثناء معالجة فعلية** (لا يُمسَك دائماً).
- **اختبار النجاح:** يبقى يمرّر بعد ٣٠ دقيقة شاشة مطفية، وبعد إعادة تشغيل الجهاز.
- **صدق:** ماكو ضمان 100% ضد كل OEM؛ الهدف أفضل صمود + شفافية مع المستخدم.
- **يحتاج أجهزة حقيقية.**

### 🪨 وحش ٢ — التوجيه (Controlled Flooding)
routing بجداول مسارات مستحيل بطوبولوجيا متحركة. الحل **فيضان مُتحكَّم به** بأربع آليات:
1. **Message ID** (هاش فريد) → dedup عبر الشبكة.
2. **TTL** (حدّ قفزات، يبدأ ~25) → ينقص كل قفزة، صفر = يتوقف. الطوارئ TTL أعلى.
3. **Seen-set** (ذاكرة الرزم المرئية) → LRU + **Bloom filter** (يقبل false-positive
   نادر، **ما يقبل false-negative** — الاتجاه الآمن).
4. **Suppression** (قلب منع العاصفة) → قبل الإعادة، انتظر تأخير عشوائي وأنصت؛ لو
   سمعت جار بثّها، اسكت.

**معاملات قابلة للضبط:** `DEFAULT_TTL=25`، `EMERGENCY_TTL=40`،
`MIN/MAX_DELAY=20/200ms`، `SUPPRESS_THRESHOLD=2`، `SEEN_SET_TTL=24–48h`.
كلها **تُعاير بمحاكاة الكثافة** (٥→١٠→٢٠+ جهاز).

- **اختبار النجاح:** رسالة تقطع ≥٥ قفزات بلا تكرار لا نهائي وبلا انفجار trafik.
- **منطق نقي قابل لاختبار JVM.**

### 🪨 وحش ٣ — التخزين والتسليم (Store-and-Forward)
ماكو سيرفر يقول "وصلت"، فكيف نعرف أوفلاين؟ عبر **آلة حالة** + **إيصالات تفيض رجوعاً**:

```
QUEUED_LOCAL → IN_MESH ─┬→ DELIVERED_PEER → ACKED → EXPIRED
                         └→ UPLOADED_SERVER ┘
```
| الحالة | المؤشر | المعنى |
|--------|--------|--------|
| QUEUED_LOCAL / IN_MESH | ⏳ | مخزونة / تُفاض الآن |
| DELIVERED_PEER / UPLOADED_SERVER | ✓ | وصلت الهدف / رُفعت للسيرفر |
| ACKED | ✓✓ | رجع إيصال للمصدر |
| EXPIRED | — | انتهت (24–48h أو وصل ACK) → تتوقف وتُشطَّب |

- كل رزمة تُخزَّن محلياً وتُعاد بثّها **دورياً** (الجيران يتغيرون). الإيصال رزمة
  `Receipt` **موقّعة** تفيض نحو `originNodeId`.
- **اختبار النجاح:** دورة ⏳→✓ تشتغل كاملة أوفلاين.
- **المنطق (آلة الحالة، المطابقة، expiry) قابل لاختبار JVM؛ الـ persistence أندرويد.**

### 🪨 وحش ٤ — التشفير والهوية
- كل عقدة تولّد محلياً (أول تشغيل) زوجين مفاتيح: **Ed25519** (توقيع/إثبات أصل) و
  **X25519** (تشفير). `NodeId` = اشتقاق من المفتاح العام للتوقيع. المفتاح الخاص
  **لا يطلع أبداً**.
- **قيد مهم:** Android Keystore ما يدعم Ed25519/X25519 جيداً → ولّدها بـ libsodium،
  وشفّرها بمفتاح **AES رئيسي** بالـ Keystore (TEE/StrongBox)، واخزن الـ ciphertext
  بـ SQLCipher.
- **E2E عبر `crypto_box_seal`** (sealed box): يشفّر لمفتاح عام بلا handshake — مثالي
  لعقد متحركة تختفي بأي لحظة. الوسطاء يمرّرون blob + توقيع، ما يفكّونه.
- **أمان السيرفر:** مفتاح السيرفر **مثبّت (pinned)** + **TLS cert pinning**.
- **طبقتا ثقة:** موثوق (QR وجهاً لوجه، يمنع MITM) يشوف الاسم/الموقع · غريب يمرّر blob فقط.
- **اختبار النجاح:** relay ما يقرأ unicast؛ التوقيع يُتحقَّق، والتزوير يُرفَض.
- **منطق التشفير قابل لاختبار JVM عبر `lazysodium-java`؛ تخزين المفاتيح أندرويد.**

### 🪨 وحش ٥ — البوابة الانتهازية
- **انتخاب لا-مركزي (gossip):** العقدة اللي تلمس نت تعلن قدرتها، وتُختار **الأقوى**
  (بطارية أعلى، إشارة أقوى، كاسر تعادل أعلى `NodeId`). الباقي يسكت؛ لو اختفت يُعاد الانتخاب.
- **رفع Idempotent:** حتى لو ارتفع تكرار، السيرفر يستبعده عبر `MessageId`. فالانتخاب
  **تحسين** مو شرط صحة.
- **جدول "ماذا يُرفع" ملزم:** التجميعي (عدد، كثافة، وجبات، ماء، إسعاف، صحة عامة،
  حركة مواكب، شفافية مواكب) ✅ مكشوف · رسائل/مواقع الأفراد 🔒 **blobs مشفّرة عبوراً
  فقط** · أي بيانات فردية بالاسم ⛔ **لا تُجمع أصلاً**.
- **اختبار النجاح:** عقدة واحدة ترفع، الباقي يسكت، والسيرفر يستبعد التكرار.
- **منطق الانتخاب قابل لاختبار JVM؛ الرفع الفعلي (HTTPS+pinning) يحتاج شبكة/أندرويد.**

### 🪨 وحش ٦ — نقل BLE
- **Advertising** لإعلان الحضور فقط (NodeId + flags)، **مو لنقل الرزم**.
- **GATT connections** للنقل الفعلي. **MTU** يُطلَب أكبر (حتى 517) لتقليل التجزئة.
- **التجزئة/التجميع:** الرزم > MTU تُقسَّم fragments (`packetId` + `idx` + `total`)،
  والمستقبل يجمّعها (timeout يُسقِط الناقص).
- **مجدول الاتصالات:** يمسك ~4–8 اتصالات ويدوّر بينها (يفضّل RSSI أقوى وأحدث ظهور).
- **Wi-Fi Aware** للباندويث الأعلى عند توفّره (اكشف التوفّر، وارجع لـ BLE عند غيابه).
- **جدول الجيران:** `NodeId`، `rssi`، `lastSeen`، `batteryHint`، `gatewayCapable`.
- **اختبار النجاح:** جهازان يكتشفون بعض ويتبادلون رزمة > MTU بالخلفية.
- **منطق التجزئة/التجميع قابل لاختبار JVM؛ الراديو الفعلي أندرويد + أجهزة.**

### 🪨 وحش ٧ — التخزين المحلي
- **Room + SQLCipher** (تشفير عند الراحة لكل القاعدة، مفتاحها محمي بالـ Keystore).
- **مصدر الحقيقة الوحيد محلي** — كل UI يقرأ من Room عبر Flow، ما ينتظر شبكة.
- **تنظيف دوري (reaping):** شطب المنتهي، تقليم seen-cache والجيران المتقادمين، وحدّ
  أقصى للحجم (الطوارئ تبقى، الأقدم يُسقَط).
- **اختبار النجاح:** كتابة/قراءة مشفّرة، وتنظيف بلا نزف ذاكرة.
- **يحتاج بيئة أندرويد (Room/SQLCipher)؛ المنطق فوقه قابل لاختبار JVM بمستودع وهمي.**

---

## ٤. الميزات (فوق البنية)

- **شخصية (E2E، للموثوقين):** الموقع، الرسائل (بمؤشرات ⏳/✓/✓✓)، المبيت.
- **تجميعية (مكشوفة، عن مكان/خدمة — لا فرد):** بيانات الموكب (وجبات، ماء،
  تواليتات، إسعاف)، صحة عامة (أرقام بكل محور)، حركة المواكب، الموردين، شفافية مالية
  المواكب، عدد الزوار/الكثافة.
- **السلامة (بشري مو آلي):** زر استغاثة (SOS) لجهة إنسان مسؤول، بلاغ خطر بمكان.

> **القاعدة الحاكمة:** التطبيق ييسّر معلومة وإرشاد وبلاغ طوارئ بشري — لا يراقب ولا
> يعاقب. والخطوط الحمراء (تتبّع فرد بالاسم، عقوبة آلية، تعرّف وجوه) **مطلقة** ولا
> تُبنى تحت أي طلب. القاعدة: رقم عن مكان/خدمة ✅، عن إنسان فرد ⛔.

---

## ٥. نموذج البيانات (Entities)

```
Identity (1) ── يملك ──> TrustedContact (N)
Message  (N) ── له ────> DeliveryReceipt (0..1)
Message  (N) ── يُفهرَس ─> SeenMessageCache
AggregateRecord (N) ── يُرفَع عبر ─> Gateway → Server
```

- **`Identity`** (سجل واحد): `nodeId`، مفاتيح التوقيع/التشفير (الخاصة **مشفّرة**)، اسم ظاهر.
- **`TrustedContact`**: `nodeId`، المفتاحان العامان، اسم ظاهر (من QR)، وقت الإضافة.
- **`Message`**: `id`(MessageId)، النوع، الأولوية، المصدر/الهدف، `payload`
  (مشفّر للـ unicast)، `signature`، `ttlHops`، `state`، أوقات الإنشاء/الانتهاء/آخر بث.
- **`SeenMessageCache`**: `messageId` + `firstSeenAt` (للـ dedup والتقليم).
- **`AggregateRecord`**: `category`، `axis` (مكان/محور **مو شخص**)، `payload`
  (أرقام)، `uploadState`. ⚠️ **لا حقل يعرّف فرداً بالاسم.**
- **`DeliveryReceipt`**: `messageId`، `ackedBy`، `signature`، `kind`.

البايتات الحساسة **مشفّرة عند الراحة** (SQLCipher)، والمفاتيح الخاصة **مشفّرة
مرتين** (Keystore فوق).

---

## ٦. تنسيق الرزمة (Wire Format)

مدمج (BLE محدود)، قابل للتوسعة (version)، ويفصل **header مكشوف** (للتوجيه) عن
**body مشفّر** (للمحتوى). Little-endian.

```
HEADER (مكشوف):
 0  1  version      1  1  type(0=BROADCAST 1=UNICAST 2=RECEIPT)
 2  1  priority     3  1  ttlHops (ينقص كل قفزة — خارج التوقيع)
 4  16 messageId    20 16 originNodeId    36 16 destNodeId
 52 8  createdAt    60 2  bodyLen
BODY:    62  N  body (sealed box للـ unicast، أو plaintext للـ broadcast)
SIGNATURE: 62+N  64  signature (Ed25519 على header*+body)
```
- `header*` = الـ header كله ما عدا البايت ٣ (`ttlHops`) يُعامَل كصفر بالتوقيع.
  **ليش:** كل relay ينقص TTL؛ لو كان ضمن التوقيع لانكسر. الباقي محمي ضد العبث.
- **التجزئة (Fragment):** `packetId(16)` + `fragIndex(2)` + `fragTotal(2)` + chunk.
- **حالات اختبار:** encode→decode يرجّع نفس الحقول · تغيير `ttlHops` ما يكسر التوقيع
  · تغيير أي حقل آخر يكسره · `bodyLen` غلط → فشل مضبوط بلا crash · split/reassemble بأطوال حدّية.

---

## ٧. خطة البناء (Milestones)

**الأنظمة القاتلة أول.** كل milestone يُبنى ويشتغل قبل التالي.

| M | العنوان | الوحوش | يحتاج أجهزة؟ |
|---|---------|--------|--------------|
| **M1** | الخلفية + النقل | ١ + ٦ | ✅ نعم |
| **M2** | التوجيه | ٢ | 🟡 منطق JVM + تأكيد بأجهزة |
| **M3** | التخزين والتسليم | ٣ + ٧ | 🟡 منطق JVM + Room بأندرويد |
| **M4** | التشفير والهوية | ٤ | 🟡 منطق JVM + Keystore بأندرويد |
| **M5** | البوابة | ٥ | 🟡 منطق JVM + شبكة |
| **M6** | الميزات | القسم ٤ | ✅ |
| **M7** | الواجهة | Compose | ✅ |

**تسلسل الفتح الأول:** توليد المفاتيح → ملف شخصي أدنى → صلاحيات بالتسلسل (BLE →
موقع → إشعارات → إعفاء بطارية) → تشغيل mesh → تخزين مشفّر → دائرة الثقة (QR) →
مزامنة انتهازية.

**هيكل المشروع:** `app/{ui, features, crypto, mesh/{routing,forward,gateway},
transport, background, data}`.

**تقسيم Gradle (يفصل المنطق النقي عن أندرويد):**
- `:core` (JVM pure Kotlin) → routing، forward state machine، crypto engine، wire
  format، fragmenter، gateway election. ← **unit tests JVM تشتغل بدون SDK.**
- `:app` (Android) → transport، background، data، KeyVault، ui. ← يعتمد على `:core`.

**تقسيم العمل** كُسِّر لـ ~134 قطعة صغيرة (~100 سطر/قطعة، ~18,000 سطر إجمالاً) عبر
الوحوش والميزات والواجهة.

---

## ٨. استراتيجية الاختبار

**يُختبَر الآن على JVM (منطق نقي، بلا أجهزة):**
| القسم | قابل JVM؟ |
|-------|-----------|
| التوجيه (TTL/dedup/suppression) | ✅ كامل |
| آلة الحالة (store-and-forward) | ✅ كامل (مستودع وهمي) |
| التشفير (seal/sign/verify) | ✅ كامل (lazysodium-java) |
| wire format / fragmentation | ✅ كامل |
| انتخاب البوابة | ✅ المنطق |
| محاكاة الكثافة المنطقية | ✅ جزئياً (in-memory graph) |
| BLE/الخلفية/Room/Keystore/UI | ⛔ يحتاج SDK + أجهزة |

**يحتاج أجهزة:** تكامل BLE (أجهزة متعددة، مو محاكي)، Room+SQLCipher، Keystore،
Foreground Service، **محاكاة كثافة فعلية ٥→١٠→٢٠+ جهاز** لمعايرة معاملات التوجيه،
صمود الخلفية ساعة + reboot عبر OEMات مختلفة، وقياس البطارية.

---

## ٩. الافتراضات (على شنو بنينا التصميم؟)

كل افتراض: **نفترض** / **لو طلع غلط شنو نسوي**.

- **أغلب الزوار أندرويد** → لو لا، نحتاج نسخة آيفون لاحقاً.
- **الناس يفعّلون التطبيق ويخلّونه بالخلفية** → لو القليل بس، الرسائل ما توصل بعيد
  → حملة توعية.
- **الناس يقبلون يمرّرون رسائل غرباء** → نوضّح إن المحتوى مشفّر وما أحد يقراه.
- **التلفونات تدعم BLE وبطاريتها تصمد ساعات** → وإلا تطلع من الشبكة / نحتاج وضع موفّر قوي.
- **فيه كثافة كافية (ناس قريبين)** → بالأماكن الفاضية نخزّن ونمرّر لمن نلقى ناس.
- **النت ينقطع لكن يُلمَس أحياناً** → لو ماكو نت أبداً، تشتغل محلياً بلا مزامنة سيرفر.
- **اللي يمرّر ما يقدر يقرأ (E2E)** → نختار مكتبة مُختبَرة (libsodium).
- **السيرفر وجهة تجميع فقط** → البيانات الشخصية مشفّرة أصلاً، حتى السيرفر ما يقراها.

**أخطر ٣ افتراضات** (لو طاحت، الفكرة بخطر): عدد كافٍ من المستخدمين · أندرويد ما
يقتل الخلفية · البطارية تصمد طول اليوم.

---

## ١٠. البيئة والحالة (الصدق أولاً)

**ماكو كود بعد — توثيق تصميم فقط.** البيئة السحابية الحالية ما تبني أندرويد:

| المورد | الحالة |
|--------|--------|
| Java (OpenJDK 21)، Gradle 8.14.3، Maven Central، GitHub | ✅ متاح |
| **Android SDK** | ⛔ غير منصَّب |
| **`dl.google.com`** (تنزيل SDK) | ⛔ محجوب |
| **`maven.google.com`** (AGP، AndroidX، Compose، Room) | ⛔ محجوب |
| أجهزة Android حقيقية | ⛔ ماكو |

**ليش حاجز:** بناء أي Android module يحتاج AGP + AndroidX (على `maven.google.com`
المحجوب) و SDK platform (من `dl.google.com` المحجوب)؛ و M1/M6 يحتاجون أجهزة متعددة.

**المسار للأمام (خيارات):**
1. **أصلح البيئة:** أضف `maven.google.com` و`dl.google.com` للـ allowlist وثبّت
   Android SDK → نبدأ بناء أندرويد فعلي (M1 أول). مرجع:
   https://code.claude.com/docs/en/claude-code-on-the-web
2. **ابنِ `:core` الآن:** نفّذ المنطق النقي (M2 + M4 + منطق M3/M5) بـ JVM tests
   خضراء فعلية، وأجّل قشرة أندرويد.
3. **توثيق فقط** (الوضع الحالي).

dependencies المتاحة من Maven Central للخيار ٢: `kotlin-stdlib`،
`kotlinx-coroutines-core`، `com.goterl:lazysodium-java`، `net.java.dev.jna:jna`،
`junit`/`kotlin-test`.

> **مبدأ التوقّف الأمين:** ما نكتب كود أندرويد ونقول "شغّال" وهو ما انبنى ولا
> انختبر. الوثائق مصمّمة كـ **spec قابل للتنفيذ مباشرة** عند توفّر البيئة الصحيحة.
